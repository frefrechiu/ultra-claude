---
phase: 09-tests-docs-examples-v010-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ultra_claude/__init__.py
  - src/ultra_claude/py.typed
  - CHANGELOG.md
autonomous: true
requirements:
  - PKG-06
must_haves:
  truths:
    - "src/ultra_claude/__init__.py declares __version__ = \"0.1.0\" (literal, single quotes or double, exactly that string)"
    - "python -c \"import ultra_claude; print(ultra_claude.__version__)\" prints 0.1.0 in an editable install"
    - "src/ultra_claude/py.typed exists as an empty file (PEP 561 type marker so downstream typed users see ultra_claude as type-checked)"
    - "CHANGELOG.md has a \"## [0.1.0] - 2026-05-02\" section listing the v1 feature surface (config + transcript + adapters + stop conditions + orchestrator + CLI + presets) and listing PKG-05 as superseded by PKG-06 (the 0.1.0 release publishes the real package, replacing the never-uploaded 0.0.1 stub)"
    - "[Unreleased] section in CHANGELOG.md is empty (or removed) -- no in-flight work after 0.1.0"
  artifacts:
    - path: "src/ultra_claude/__init__.py"
      provides: "Package version literal consumed by hatchling [tool.hatch.version] path"
      contains: "__version__ = \"0.1.0\""
    - path: "src/ultra_claude/py.typed"
      provides: "PEP 561 marker telling type checkers ultra_claude is fully typed"
      min_lines: 0
    - path: "CHANGELOG.md"
      provides: "Versioned release notes (Keep a Changelog format)"
      contains: "## [0.1.0]"
  key_links:
    - from: "pyproject.toml [tool.hatch.version]"
      to: "src/ultra_claude/__init__.py"
      via: "path = \"src/ultra_claude/__init__.py\""
      pattern: "__version__\\s*=\\s*\"0\\.1\\.0\""
    - from: "src/ultra_claude/cli.py"
      to: "ultra_claude.__version__"
      via: "from . import __version__ -> click.version_option(version=__version__, ...)"
      pattern: "version_option\\(version=__version__"
---

<objective>
Bump the package version from `0.0.1` (the never-uploaded Phase 1 stub literal) to `0.1.0` (the real first functional release), drop a PEP 561 `py.typed` marker so downstream users see ultra_claude as fully typed, and flesh out the CHANGELOG `[0.1.0]` section with the v1 feature surface.

Purpose: Land the three lightweight prerequisites that every later 09-* plan depends on (the build artifacts in 09-04 carry the new version; the README in 09-03 cites 0.1.0 in its install command). Foundation step. Implements D-01 (version bump) and D-02 (py.typed) from CONTEXT.md. Closes PKG-06's first half (the literal); PKG-06's release-side closure happens in 09-04 via `python -m build`.

Output: Three on-disk artefacts (`src/ultra_claude/__init__.py` modified, `src/ultra_claude/py.typed` created, `CHANGELOG.md` modified). Zero source-code behaviour changes; pure metadata + version-string editing. 83/83 prior tests must still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tests-docs-examples-v010-release/09-CONTEXT.md
@CLAUDE.md
@CHANGELOG.md
@src/ultra_claude/__init__.py
@pyproject.toml

<interfaces>
<!-- Keep this section short - the only external coupling this plan has is to
     hatchling's dynamic-version mechanism declared in pyproject.toml. -->

From pyproject.toml lines 71-72:
```toml
[tool.hatch.version]
path = "src/ultra_claude/__init__.py"
```

This means hatchling reads the `__version__ = "..."` literal directly from
`src/ultra_claude/__init__.py` at build time. The change in Task 1 below MUST
preserve the exact regex `__version__\s*=\s*"X.Y.Z"` shape -- no f-strings, no
imports, no dynamic computation. A bare literal-string assignment.

From src/ultra_claude/cli.py line 44 + 267:
```python
from . import __version__
@click.version_option(version=__version__, prog_name="ultra-claude")
```

After this plan lands, `ultra-claude --version` (the click flag from CLI-01)
will print `ultra-claude, version 0.1.0` instead of `... 0.0.1`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump __version__ from 0.0.1 to 0.1.0 in src/ultra_claude/__init__.py</name>
  <files>src/ultra_claude/__init__.py</files>
  <action>
Edit `src/ultra_claude/__init__.py` so the literal `__version__` line reads exactly:

```python
"""ultra-claude — orchestrate Claude Code, Gemini CLI, and Codex CLI in a multi-agent debate using existing CLI logins (no API keys)."""

__version__ = "0.1.0"
```

Do NOT change anything else (the docstring stays exactly as-is, including the em-dash). Do NOT add imports, do NOT add `__all__`, do NOT compute the version dynamically -- hatchling's `[tool.hatch.version] path = "src/ultra_claude/__init__.py"` (pyproject.toml line 72) does a regex extraction of the raw string literal at build time, so any indirection breaks the build.

Use the Edit tool, not Write -- the file is two lines and we are changing exactly the version literal.

Cross-platform note: the file MUST stay LF-only on disk despite the `core.autocrlf=true` host setting. The Edit tool preserves existing line endings, so as long as the prior file is LF-only (which Phase 1 verified), no extra step is needed. After the Edit, run a sanity probe:

```bash
python -c "import ultra_claude; print(ultra_claude.__version__)"
# Expected: 0.1.0
```

(This works in the editable install set up by Phase 1.)
  </action>
  <verify>
    <automated>python -c "import ultra_claude; assert ultra_claude.__version__ == '0.1.0', ultra_claude.__version__; print('VERSION OK')"</automated>
  </verify>
  <done>
- `src/ultra_claude/__init__.py` contains the literal `__version__ = "0.1.0"` (verified by `grep -E '__version__\s*=\s*"0\.1\.0"' src/ultra_claude/__init__.py` returning a match)
- `python -c "import ultra_claude; print(ultra_claude.__version__)"` prints `0.1.0` (no exception, no import error)
- The docstring on line 1 is unchanged (em-dash + parentheses preserved -- pre-edit `wc -c` minus 6 bytes equals post-edit `wc -c` because `0.0.1` -> `0.1.0` is byte-equal but a non-trivial sanity check is "the file still parses as valid Python and has the same module docstring")
- 83/83 existing test suite still passes (the version literal is not asserted by any test today, but the byte-equal version-bump must not trigger any regression in pytest collection)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create the PEP 561 py.typed marker</name>
  <files>src/ultra_claude/py.typed</files>
  <action>
Create a new EMPTY file at `src/ultra_claude/py.typed`. The file's contents are not meaningful -- the FILE'S EXISTENCE is the PEP 561 signal to downstream type checkers that `ultra_claude` ships type information inline.

Use the Write tool with empty content (zero bytes), or write a single newline. Per CONTEXT.md decision "py.typed marker -- recommend: yes (types are valuable; one-line file)" the contents may be empty or a single newline -- both are valid PEP 561.

Recommended: empty file (zero bytes). Rationale: `mypy` walks the package tree and detects `py.typed` by name; it never reads the contents. An empty file is the smallest possible artifact.

Cross-platform note: on Windows the working tree may show a trailing CRLF if the user's git autocrlf=true is set; this is OK -- the marker file's bytes are not consumed by anything, only its presence on disk under the package root matters.

Hatchling packaging note: pyproject.toml line 75 says `[tool.hatch.build.targets.wheel] packages = ["src/ultra_claude"]`. This means hatchling includes the entire `src/ultra_claude/` tree (including `py.typed`) in the built wheel automatically. No manifest tweaks are required.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; p = Path('src/ultra_claude/py.typed'); assert p.exists() and p.is_file(), 'py.typed must exist as a file'; print('PY.TYPED OK')"</automated>
  </verify>
  <done>
- `src/ultra_claude/py.typed` exists at the canonical PEP 561 location (sibling of `__init__.py`)
- `Path('src/ultra_claude/py.typed').is_file()` is `True`
- The file is empty OR contains a single newline (both are PEP 561 valid)
- `python -m build` (run later in 09-04) will include `py.typed` in the wheel automatically because of pyproject.toml line 75
  </done>
</task>

<task type="auto">
  <name>Task 3: Flesh out CHANGELOG.md [0.1.0] section dated 2026-05-02</name>
  <files>CHANGELOG.md</files>
  <action>
Replace the entire current contents of `CHANGELOG.md` with the following (use the Write tool):

```markdown
# Changelog

All notable changes to ultra-claude are documented here. The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [0.1.0] - 2026-05-02

First functional release. The `0.0.1` literal in the previous heading was a never-uploaded name-reservation stub -- this `0.1.0` is the first version actually published to PyPI as a working package.

### Added

- **CLI surface** (`ultra-claude run` / `doctor` / `--version` / `--help`).
  - `run TASK_FILE` reads a task from a file and drives a multi-agent debate (CLI-03).
  - `run --inline "<task>"` accepts the task as a string (CLI-06).
  - `run --config <path>` overrides the default `./ultra-claude.yaml` (CLI-04).
  - `run --preset debate` loads the bundled 3-agent preset without a local YAML (CLI-05).
  - `run --dry-run` validates config and prints the planned turn order without invoking any adapter (CLI-07).
  - `run --output <path>` overrides the transcript output path (CLI-08).
  - `run --abort-on-error` halts on the first adapter failure instead of the default continue-on-error mode.
  - `doctor` probes `claude` / `gemini` / `codex` on PATH and prints a per-CLI status table (CLI-09).
  - Exit codes follow Unix convention: 0 on success, 1 on adapter/runtime error, 2 on config validation error (CLI-10).
  - Live progress to stderr only when stdout AND stderr are TTYs; suppressed when piped or redirected (CLI-11).
- **Config schema** (`ultra-claude.yaml`).
  - Pydantic v2 `RoundtableConfig` and `AgentConfig` with strict validation (CFG-01, CFG-02).
  - Required agent fields: `name`, `role`, `adapter` (Literal `claude`/`gemini`/`codex`), `system_prompt` (CFG-02).
  - Defaults: `max_turns=12`, `stop_keywords=["AGREED", "DONE"]`, `turn_order="round_robin"` (CFG-04, CFG-05).
  - Validation errors print one line per offending field path (e.g. `agents[0].adapter: invalid value 'clade'`); no Python tracebacks reach the user (CFG-03).
- **Transcript module**.
  - Append-as-you-go markdown writer with `<!-- turn:N agent:Name -->` HTML-comment sentinels so re-prompting does not collide with content (TRX-01, TRX-02).
  - JSONL sidecar at `<transcript>.jsonl`, one record per turn, fields `turn`/`agent`/`role`/`prompt_hash`/`output` (TRX-03).
  - LF-only newlines and UTF-8 encoding on every platform (TRX-04, TRX-05).
- **Adapter Protocol and three concrete adapters**.
  - `Adapter` `typing.Protocol` (structural subtyping; third parties do not inherit) (ADP-01).
  - `_SubprocessAdapterMixin` enforces the safe-subprocess contract: stdin-piped prompts (Pitfall #1 / Windows ~8 KB cmd.exe argv limit), `text=True` + `encoding="utf-8"` + `errors="replace"` (Pitfall #3 / cp1252 crash), mandatory timeout, list-form argv, `shell=False` (ADP-02).
  - Empty-stdout defense raises `AdapterError` when `returncode == 0` and `stdout.strip() == ""` -- catches the live `codex exec` 0.124.0+ TTY bug ([openai/codex#19945](https://github.com/openai/codex/issues/19945)) (ADP-03 / Pitfall #2).
  - Cross-platform process-tree kill on `TimeoutExpired`: POSIX `os.killpg` after `start_new_session=True`; Windows `taskkill /T /F /PID` after `CREATE_NEW_PROCESS_GROUP` (ADP-04 / Pitfall #5).
  - `AdapterAuthError` (subclass of `AdapterError`) on `FileNotFoundError` or auth-marker substring match in stdout/stderr (ADP-08).
  - `ClaudeAdapter` (`claude -p`), `GeminiAdapter` (`gemini -p`), `CodexAdapter` (`codex exec`), all sharing the mixin (ADP-05, ADP-06, ADP-07).
- **Stop conditions**.
  - `Keyword` with anchored multiline regex (`^<keyword>\s*$`) and unanimity-window default `n=2`/`m=2` so a single agent saying the marker cannot stop the run (STP-02, STP-03 / Pitfall #4).
  - `MaxTurns` (STP-04) and `AnyOf` composite with short-circuit semantics (STP-05).
- **Orchestrator loop** (`run(config, task) -> Path`).
  - Round-robin agent iteration (ORC-01, ORC-02).
  - Per-turn prompt assembly: task header + agent system prompt + transcript-so-far + GOAL ANCHOR re-injection of the task + agent-name reminder (ORC-03 / Pitfall #6).
  - Continue-on-error by default (placeholder turn `[adapter error: <exc>]` appended); `abort_on_error: true` re-raises (ORC-05).
  - Structured stderr logging via `logging.getLogger("ultra_claude.orchestrator")`; stdout reserved for the CLI to print the transcript path (ORC-06).
- **Bundled preset**: `presets/debate.yaml` -- 3-agent roundtable (Architect: claude, Critic: gemini, Implementer: codex) with `max_turns=9` and `stop_keywords=["AGREED", "SHIP IT"]` (PRE-01).
- **PEP 561 type information** via the `py.typed` marker -- downstream typed users see `ultra_claude` as fully type-checked.
- **Documentation**: full `README.md` (1-line pitch, GIF placeholder, 3-command quickstart, "why this exists", config example, "extending to new CLIs") and `CONTRIBUTING.md` (dev setup, how to add an adapter, v1 policy) (DOC-01, DOC-02).
- **Examples directory** (`examples/`): synthetic 3-turn transcript captured from the FakeAdapter test infrastructure plus the YAML config that produced it (PRE-02).
- **Test suite** runs in a clean virtualenv with NONE of `claude`/`gemini`/`codex` installed: adapter tests use `pytest-subprocess`'s `fp` fixture; orchestrator E2E tests use `tests/fixtures/echo_cli.py` as a fake CLI; `pytest-cov` reports coverage on `src/ultra_claude/` (TST-01..04).
- **CI lint tripwire** (`tests/test_subprocess_lint.py`): AST-walks `src/ultra_claude/` and fails the build if any `subprocess.run` / `subprocess.Popen` call is missing the safe-contract kwargs or sets `shell=True` (TST-05; landed in Phase 4).

### Changed

- Replaced the stub `README.md` (Phase 1 name-reservation placeholder) with the full v0.1.0 README.

### Notes

- The `0.0.1` PyPI stub release referenced by Phase 1's PKG-05 was never uploaded by the user. This `0.1.0` release supersedes it: a fresh PyPI account upload of `0.1.0` claims the `ultra-claude` distribution name AND ships the functional package in a single step. `python -m build` produces `dist/ultra_claude-0.1.0-py3-none-any.whl` and `dist/ultra_claude-0.1.0.tar.gz`; `twine upload dist/ultra_claude-0.1.0*` (user action) closes PKG-06.
- This release is manual: `python -m build` + `twine upload`. Trusted Publishing (PyPI OIDC) is deferred to v2.

## [0.0.1] - 2026-05-02

### Added
- PyPI name reservation stub. No functional code; the `0.0.1` release exists only to claim the `ultra-claude` distribution name on PyPI before any public mention (per `.planning/research/PITFALLS.md` Pitfall 14).
- Initial repository scaffolding: `LICENSE` (MIT), `.gitignore`, `README.md` stub, `pyproject.toml` (hatchling build backend), `src/ultra_claude/__init__.py` exposing `__version__`.
```

Cross-platform writing note: `core.autocrlf=true` on Windows may convert LF -> CRLF in the working tree. Counter this by using `Path.write_bytes(content.encode("utf-8"))` (NOT `write_text`) so the bytes hit disk verbatim. The `Write` tool typically writes via Python's text mode which on Windows could insert CRLFs; use the Bash + `python -c` route if Write produces CRLF:

```bash
python -c "from pathlib import Path; Path('CHANGELOG.md').write_bytes(open('CHANGELOG.md.tmp','rb').read())"
```

After writing, verify LF-only on disk:

```bash
python -c "from pathlib import Path; data = Path('CHANGELOG.md').read_bytes(); assert b'\\r\\n' not in data, 'CRLF found'; print('LF-only OK')"
```

(If CRLFs ARE present and you cannot eliminate them via `Path.write_bytes`, fall back to Bash heredoc to a Unix line-ending temp file then move into place.)
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; data = Path('CHANGELOG.md').read_bytes(); assert b'\r\n' not in data, 'CRLF found in CHANGELOG.md'; assert b'## [0.1.0] - 2026-05-02' in data, 'missing [0.1.0] heading'; assert b'openai/codex#19945' in data, 'missing codex bug citation'; print('CHANGELOG OK')"</automated>
  </verify>
  <done>
- `CHANGELOG.md` contains a heading line `## [0.1.0] - 2026-05-02` (exact match)
- `CHANGELOG.md` lists the 11 v1 requirement areas (CLI / Config / Transcript / Adapters / Stop / Orchestrator / Preset / Docs / Examples / Tests / py.typed) under `### Added`
- `CHANGELOG.md` cites `openai/codex#19945` for Pitfall #2 / ADP-03
- `CHANGELOG.md` mentions that `0.1.0` supersedes the never-uploaded `0.0.1` stub
- `CHANGELOG.md` is LF-only on disk (no CRLF bytes)
- The `[0.0.1]` section from the prior file is preserved at the bottom (not deleted -- Keep a Changelog convention is to retain history)
- The `[Unreleased]` heading is present but empty (next-release placeholder)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edit tool -> on-disk file | The agent's edits cross from agent intent to the user's filesystem; bytes here are subsequently consumed by hatchling at build time and by every Python import. |
| __init__.py -> hatchling regex | The `[tool.hatch.version] path = ".../__init__.py"` mechanism reads the file as text and matches a regex; non-literal version expressions break this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | src/ultra_claude/__init__.py version literal | mitigate | Edit ONLY the literal `__version__ = "0.0.1"` -> `__version__ = "0.1.0"`. Preserve docstring. Verify via `python -c "import ultra_claude; assert ultra_claude.__version__ == '0.1.0'"`. |
| T-09-02 | Tampering | CHANGELOG.md historical entries | mitigate | Preserve the existing `## [0.0.1] - 2026-05-02` section verbatim at the bottom (Keep a Changelog convention). Adding `## [0.1.0]` heading above it -- never deleting the prior heading. |
| T-09-03 | Information Disclosure | py.typed file contents | accept | The PEP 561 marker has no contents to disclose; an empty file is the canonical form. No PII, no secrets. Low-value target. |
| T-09-04 | Denial of Service | hatchling build pipeline | mitigate | The version literal must remain a bare double-quoted string for hatchling's regex to extract it. Verify via `python -m build --sdist --no-isolation` smoke test (deferred to 09-04 -- this plan only validates the runtime import). |
</threat_model>

<verification>
After all three tasks land, run the following in sequence and require all to PASS:

```bash
# 1. Version literal extracted correctly by Python
python -c "import ultra_claude; assert ultra_claude.__version__ == '0.1.0'; print('VERSION OK')"

# 2. py.typed marker present
python -c "from pathlib import Path; assert Path('src/ultra_claude/py.typed').is_file(); print('PY.TYPED OK')"

# 3. CHANGELOG well-formed
python -c "from pathlib import Path; data = Path('CHANGELOG.md').read_bytes(); assert b'\r\n' not in data; assert b'## [0.1.0] - 2026-05-02' in data; print('CHANGELOG OK')"

# 4. Full test suite still passes (zero regression)
pytest -x

# 5. mypy still clean
mypy

# 6. ruff still clean
ruff check
```

If any of those fail, fix before committing. The `pytest -x` run is the canary -- 09-01 changes nothing that any test asserts, so a regression points at an unrelated import breakage from the version-literal change (extremely unlikely but the cheapest possible safety net).
</verification>

<success_criteria>
- `src/ultra_claude/__init__.py` literal updated to `"0.1.0"` (verified by import + grep)
- `src/ultra_claude/py.typed` exists as a file (PEP 561 marker)
- `CHANGELOG.md` has `[0.1.0] - 2026-05-02` section listing the v1 feature surface
- `[Unreleased]` heading present but empty (no in-flight work after 0.1.0)
- LF-only on disk for both edited files
- 83/83 prior tests still pass
- mypy --strict on src/ultra_claude clean
- ruff check clean
- 3 atomic commits (one per task is the cleanest -- Task 1 / Task 2 / Task 3 -- but a single squashed commit is acceptable if the executor prefers)
</success_criteria>

<output>
After completion, create `.planning/phases/09-tests-docs-examples-v010-release/09-01-SUMMARY.md` documenting:
- The exact bytes changed in `__init__.py` (one literal: `0.0.1` -> `0.1.0`)
- The size of the new `py.typed` file (expected: 0 bytes or 1 newline)
- The line count and byte count of the new `CHANGELOG.md`
- Verification gate results (5 checks above)
- Any Rule-N deviations encountered (autocrlf compensation if needed)
</output>
</content>
</invoke>
---
gsd_state_version: 1.0
milestone: v0.1.0
milestone_name: Release
status: phase-7-fully-closed-07-02-test-suites-landed-adp-06-and-adp-07-implementation-and-test-verified
last_updated: "2026-05-02T05:24:33Z"
progress:
  total_phases: 9
  completed_phases: 6
  total_plans: 14
  completed_plans: 14
  percent: 100
---

# State: ultra-claude

**Last Updated:** 2026-05-02

## Project Reference

**Core Value:** A user can run `ultra-claude run task.md` and get three CLI agents debating their problem in a transcript file — using only their existing CLI logins, no API keys.

**Current Focus:** Plan 07-02 COMPLETE (2/2 tasks). **Phase 7 fully CLOSED** (2/2 plans complete). New files `tests/test_adapter_gemini.py` (261 lines, 10132 bytes, LF-only, ASCII-only — 6 test functions, 11 collected) and `tests/test_adapter_codex.py` (333 lines, 13478 bytes, LF-only, ASCII-only — 7 test functions, 11 collected) mirror `tests/test_adapter_claude.py` 1:1 with vendor swaps to `["gemini", "-p"]` and `["codex", "exec"]` argv shapes. Both files exclusively use the `pytest-subprocess` `fp` fixture (happy paths, empty-stdout, auth markers, timeout) and `monkeypatch.setattr(subprocess, "Popen", ...)` (FileNotFoundError simulation only) -- ZERO real CLI launches, ZERO direct `subprocess.run`/`Popen` calls in the test files (TST-05 lint stays clean). Both files cover the same 6 paths: stdin pipe + trimmed stdout (Pitfall #1 mitigation), list-form argv (defensively asserted via `fp.calls`), empty-stdout-with-rc-0 -> AdapterError naming the CLI + referencing empty/19945 (Pitfall #2 inherited mixin defense), whitespace-only stdout same defense, FileNotFoundError -> AdapterAuthError with re-auth/install hint (ADP-08 path 1), parametrized auth-marker substring case-insensitive matching including uppercase variant + vendor-specific marker with backticks (ADP-08 path 2), TimeoutExpired -> `_kill_process_tree` recorded via monkeypatch + AdapterError naming the CLI + "tim" (Pitfall #5). The Gemini auth-marker parametrize has 5 entries (4 markers + 1 case variant); Codex has 4 entries (3 markers + 1 case variant). The CodexAdapter test file ALSO contains the **headline** `test_codex_empty_stdout_bug_regression` at line 129 that explicitly documents Pitfall #2 / openai/codex#19945 with: (a) bug source URL `https://github.com/openai/codex/issues/19945` in docstring, (b) bug shape pinned via `fp.register(["codex", "exec"], stdout="", stderr="warning: no TTY attached; using non-interactive mode", returncode=0)`, (c) defense location named (`_SubprocessAdapterMixin._run_subprocess` from Phase 4 plan 04-01), (d) explicit "what the test pins vs deliberately does NOT pin" section so the test is decoupled from upstream bug-fix lifecycle. The headline regression test passes WITHOUT any Codex-specific defensive code in `CodexAdapter` -- the inherited Phase 4 mixin's `if proc.returncode == 0 and not stdout.strip(): raise AdapterError(...)` line catches the bug shape directly. Two assertion anchors: (1) `"codex" in msg.lower()`, (2) `"empty" in msg.lower() or "19945" in msg`. Atomic commits: `4377a27` (test(07-02): add GeminiAdapter test suite covering ADP-06) + `e538e88` (test(07-02): add CodexAdapter test suite covering ADP-07 and openai/codex#19945 regression). All 13 plan-level verification gates PASS: `pytest tests/test_adapter_gemini.py -x -v` 11/11 PASS; `pytest tests/test_adapter_codex.py -x -v` 11/11 PASS; `pytest tests/test_adapter_codex.py::test_codex_empty_stdout_bug_regression -x -v` 1/1 PASS; **`pytest tests/` 72/72 PASS (50 prior + 22 new = 72; zero regression)**; `ruff check tests/test_adapter_gemini.py tests/test_adapter_codex.py` clean; `pytest tests/test_subprocess_lint.py -x` 3/3 PASS (TST-05 lint catches no new violations); LF-only + ASCII-only on disk on both new files; staged blobs LF-only despite `core.autocrlf=true` (gemini blob 10132 bytes / 0 CRLF / 261 LF; codex blob 13478 bytes / 0 CRLF / 333 LF); `def test_codex_empty_stdout_bug_regression` present at line 129; `openai/codex#19945` literal present 3 times in codex test file (module docstring, comment block, regression-test docstring as URL); zero deletions across both task commits; both files independently runnable in isolation; `from __future__ import annotations` in both. **Zero Rule-N deviations** -- plan executed verbatim. Note: the plan's English prose said "10 collected tests total" for each file, but the actual file as specified produces 11 each (the prose miscounted by 1; the parametrize blocks have 5 entries gemini / 4 entries codex which when added to the 5 / 6 standalone functions sum to 11, not 10). The truth-list count of FUNCTIONS (6 / 7) is correct; the COLLECTED count of 10 in the prose was an off-by-one. The actual full-suite delta is +22 (11 gemini + 11 codex), giving 72/72 instead of the prose's 70/70. Both deltas are immaterial: every test passes, every must-have is satisfied. **ADP-06 and ADP-07 are now both IMPLEMENTATION-verified (07-01 commits 4a09f27 + 5f067c1) AND TEST-verified (07-02 commits 4377a27 + e538e88), closing Phase 7 fully.** Phase 8 (CLI Surface) is now unblocked. Phase 1 autonomous portion still complete; PKG-05 still pending user `twine upload`.

**Previous (07-01) Focus:** Plan 07-01 COMPLETE (2/2 tasks). Phase 7 implementation side closed; test side awaited plan 07-02. New files `src/ultra_claude/adapters/gemini.py` (94 lines, 4123 bytes, LF-only, ASCII-only) and `src/ultra_claude/adapters/codex.py` (100 lines, 4339 bytes, LF-only, ASCII-only) defined `class GeminiAdapter(_SubprocessAdapterMixin)` (`name="gemini"`, `cli_name="gemini"`, 4-marker `auth_error_markers` tuple, one-line `invoke()` delegating to `_run_subprocess(["gemini", "-p"], prompt, timeout)`) and `class CodexAdapter(_SubprocessAdapterMixin)` (`name="codex"`, `cli_name="codex"`, 3-marker `auth_error_markers`, one-line `invoke()` delegating to `_run_subprocess(["codex", "exec"], prompt, timeout)`; module docstring references `openai/codex#19945` per D-03 to document why the inherited Pitfall #2 empty-stdout defense matters specifically here — no Codex-specific defensive code added because the Phase 4 mixin already catches the regression for every adapter). `src/ultra_claude/adapters/__init__.py` extended from 3-entry to 5-entry `__all__` in roadmap-introduction order (Adapter, _SubprocessAdapterMixin, ClaudeAdapter, GeminiAdapter, CodexAdapter) with `# noqa: RUF022` on the opening bracket line; alphabetical import-block ordering produces .base / .claude / .codex / .gemini above `__all__`. `src/ultra_claude/registry.py` replaces the `NotImplementedError` branch with concrete `if adapter_kind == "gemini": return GeminiAdapter()` / `if adapter_kind == "codex": return CodexAdapter()` branches; module docstring tense updated from forward-looking ("Phase 7 will extend...") to present/past ("After Phase 7 ... all three adapter literals return real instances"); Raises: section drops `NotImplementedError`. Atomic commits: `4a09f27` (feat(07-01): add GeminiAdapter and CodexAdapter) + `5f067c1` (feat(07-01): wire Gemini/Codex into adapters package and registry). One Rule-N deviation: **Rule 3 - Blocking**: ruff 0.15.8 RUF022 + RUF100 dual-fire on the plan-suggested closing-bracket placement of `# noqa: RUF022` for a multi-line `__all__`. Out-of-scope (NOT actioned): 4 pre-existing ruff errors in Phase 2 files (`config.py` RUF022 + UP037; `tests/test_config.py` I001 + F401), confirmed pre-existing via `git stash` round-trip; logged at `.planning/phases/07-gemini-codex-adapters/deferred-items.md`. ADP-06 and ADP-07 marked complete in REQUIREMENTS.md (implementation level — test verification by plan 07-02 NOW CLOSED).

**Previous (06-02) Focus:** Phase 6 fully CLOSED (2/2 plans complete). Plan 06-02 landed `tests/test_orchestrator.py` (460 lines, 17306 bytes, LF-only, ASCII-only) with `class FakeAdapter` (40 lines, pure-Python Protocol satisfier) plus 8 test functions covering every ORC-01..ORC-06 path: round-robin order + turn count, GOAL ANCHOR prompt assembly with task-in-header AND task-in-footer, transcript-so-far visibility by turn N+1, Keyword unanimity n=2/m=2 early-stop, AdapterError continue-on-error placeholder turn, abort_on_error=True bare-raise propagation, return-Path correctness with sentinel-comment count check, and stdout+caplog discipline. All 8 tests use the `adapter_factory` injection seam (landed in 06-01) so zero subprocess launches anywhere in the test file. **Total project test count: 42 -> 50 (zero regression)** in the 42 prior tests. Atomic commit: `747f003` (test(06-02): add orchestrator test suite covering ORC-01..ORC-06). All plan-level verification gates PASS: `pytest tests/test_orchestrator.py -x -v` 8/8 PASS; `pytest tests/` 50/50 PASS; `ruff check tests/test_orchestrator.py` clean; `mypy --strict src/ultra_claude` clean (10 source files — unchanged from 06-01); TST-05 lint test still passes (free regression check); LF-only on disk (0 CRLF / 460 LF); ASCII-only on disk; staged blob LF-only (17306 bytes / 0 CRLF / 460 LF) despite `core.autocrlf=true`; exactly 8 test functions enumerated by `re.findall(r'^def (test_\w+)\(', src, re.MULTILINE)`. Two Rule-N deviations during execution: (1) **Rule 1 - Bug**: test 8 (`test_run_logs_progress_to_stderr_only`) initially failed with `assert 'starting roundtable' in '' (where '' = CaptureResult(out='', err='').err)` — diagnosis: pytest's built-in logging plugin attaches a handler at the root logger level that captures records through the logging machinery rather than letting them reach `sys.stderr` for capsys to see. Confirmed by re-running with `-p no:logging` (passed). The plan's verification note suggested `propagate = False` after the handler reset, but that fix would NOT work because the issue is pytest's logging plugin diversion, not propagation. **Replaced** the `assert "..." in captured.err` form with `caplog: pytest.LogCaptureFixture` + `caplog.set_level(logging.INFO, logger="ultra_claude.orchestrator")` + `assert "..." in caplog.text` + `len(orchestrator_records) >= 3` for tightened assertion. Kept `assert captured.out == ""` for the stdout-discipline check (capsys IS the right tool for that — direct stdout writes are what we want to forbid). This is the pytest-canonical pattern for asserting logging output and supersedes the plan's `propagate = False` recommendation. (2) **Rule 3 - Blocking**: ruff I001 ("Import block is un-sorted or un-formatted") — required removing one blank line between the imports and the first `# ---` comment block (ruff wanted exactly one blank line, not two). No semantic change. Both fixes were applied before the commit, so commit `747f003` already reflects the corrected form. **Phase 6 fully closed.** ORC-01..ORC-06 all complete (boxes checked in REQUIREMENTS.md). 2/2 plans complete: 06-01 (commits `8cfee40` + `b9b80b3` — registry.py + orchestrator.py implementation, 363 lines) + 06-02 (commit `747f003` — 8-test FakeAdapter suite, 460 lines). **Phase 7 (Gemini + Codex adapters) is unblocked** and can proceed: it reuses the Phase 4 mixin so it doesn't touch orchestrator code; the orchestrator's adapter_factory injection seam is now test-locked, so any future regression in 06-01's `run()` shape is caught immediately by these tests. Phase 1 autonomous portion still complete; PKG-05 still pending user `twine upload`.

## Current Position

Phase: 07-gemini-codex-adapters — **COMPLETE** (2/2 plans; ADP-06 + ADP-07 IMPLEMENTATION-verified AND TEST-verified). Phase 8 (CLI Surface) is the next executor target.
Next phase: Phase 8 (CLI Surface & `debate` Preset) — depends on Phases 6 (orchestrator) and 7 (all three adapters). All prerequisites satisfied. CLI-01..CLI-11 + PRE-01 are 12 requirements to plan/execute. Plan via `/gsd-plan-phase 8`.
| Field | Value |
|-------|-------|
| Phase | 7 (Gemini + Codex Adapters) — **COMPLETE** (2/2 plans, 2/2 ADP requirements implementation+test verified) |
| Plan | 07-01 COMPLETE (commits `4a09f27` + `5f067c1`); 07-02 COMPLETE (commits `4377a27` + `e538e88`) |
| Status | ADP-06 and ADP-07 fully closed: implementation (gemini.py + codex.py + adapters/__init__.py + registry.py wiring landed in 07-01) AND tests (test_adapter_gemini.py + test_adapter_codex.py incl. headline test_codex_empty_stdout_bug_regression landed in 07-02). 72/72 full test suite PASS (zero regression — 50 prior + 22 new tests = 72); `mypy --strict src/ultra_claude` clean (12 source files); `ruff check` clean on all 4 source files + 2 new test files; `pytest tests/test_subprocess_lint.py` 3/3 PASS (TST-05 lint catches no new violations because new test files use `fp` fixture + `monkeypatch` only — zero direct subprocess imports); LF-only + ASCII-only on disk on all 6 files (4 source from 07-01 + 2 tests from 07-02); codex.py module docstring AND test_adapter_codex.py both reference `openai/codex#19945` per locked decision D-03 (3 occurrences in codex test file: module docstring + comment block + regression-test docstring URL). Phase 7 fully CLOSED. |
| Progress | 6/9 phases complete (Phases 2 + 3 + 4 + 5 + 6 + 7 fully closed; Phase 1 awaits user twine upload to fully close PKG-05); 14/14 completed plans (Phase 1: 3/3, Phase 2: 2/2, Phase 3: 1/1, Phase 4: 3/3, Phase 5: 1/1, Phase 6: 2/2, Phase 7: 2/2) |

```
[##############     ] Phase 7 fully closed (2/2 plans); Phase 8 (CLI Surface) is the next planner target
```

## Performance Metrics

| Metric | Value |
|--------|-------|
| Phases planned | 9 |
| Phases complete | 6 (Phases 2 + 3 + 4 + 5 + 6 + 7 fully closed; Phase 1 awaiting user twine upload to fully close PKG-05) |
| Plans complete | 14 (Phase 1: 3/3, Phase 2: 2/2, Phase 3: 1/1, Phase 4: 3/3, Phase 5: 1/1, Phase 6: 2/2, Phase 7: 2/2) |
| v1 requirements mapped | 58/58 |
| Requirements completed | 34 (PKG-02, PKG-03, PKG-04, PKG-07, CFG-01..05, TRX-01..05, ADP-01..08 [ADP-06 and ADP-07 now both IMPLEMENTATION + TEST verified], TST-05, STP-01..05, ORC-01..06) |
| Requirements partial | 0 |
| Requirements deferred-to-user | 1 (PKG-05 — twine upload, runbook ready) |
| Coverage | 100% |
| Plan 01-01 duration | ~2 min (2026-05-02T01:48:41Z → 2026-05-02T01:50:25Z) |
| Plan 01-02 duration | ~1.5 min (2026-05-02T01:54:42Z → 2026-05-02T01:56:12Z) |
| Plan 01-03 duration | ~5 min (2026-05-02T02:00:57Z → 2026-05-02T02:06:15Z) — autonomous portion |
| Plan 02-01 duration | ~2 min (2026-05-02T02:28:34Z → 2026-05-02T02:30:33Z) |
| Plan 02-02 duration | ~4 min (2026-05-02T02:35:56Z → 2026-05-02T02:39:52Z) |
| Plan 03-01 duration | ~6 min (2026-05-02T02:53:30Z → 2026-05-02T02:59:22Z) — 2 tasks, 2 commits, 1 Rule 1 deviation (Python 3.11 f-string limitation) |
| Plan 04-01 duration | ~7 min (2026-05-02T03:17:46Z → 2026-05-02T03:24:44Z) — 2 tasks, 2 commits, 4 Rule 3 deviations (mypy Popen overload via per-platform branches; mypy attr-defined for POSIX-only os attrs; ruff SIM105 -> contextlib.suppress; ruff RUF022 -> noqa with justifying comment) |
| Plan 04-02 duration | ~3 min (2026-05-02T03:31:04Z → 2026-05-02T03:34:05Z) — 2 tasks, 2 commits, 2 Rule 3 deviations (docstring rephrased to avoid literal `import subprocess` substring; ruff RUF022 -> noqa with justifying comment) |
| Plan 04-03 duration | ~6 min (2026-05-02T03:42:27Z → 2026-05-02T03:48:18Z) — 3 tasks, 3 commits, 2 Rule 3 deviations (ruff SIM102 nested-if combined; ruff I001 import-block formatting auto-fixed); 20 new tests (7 base + 10 claude + 3 lint); 36/36 full suite PASS |
| Plan 05-01 duration | ~3 min (2026-05-02T04:02:14Z → 2026-05-02T04:05:28Z) — 2 tasks, 2 commits, zero Rule-N deviations (plan executed exactly as written; anticipated `# noqa: RUF022` applied as documented); 6 new tests covering STP-01..05 + Pitfall #4; 42/42 full suite PASS |
| Plan 06-01 duration | ~30 min (2026-05-02T03:58:00Z → 2026-05-02T04:27:47Z) — 2 tasks, 2 commits, zero Rule-N deviations (plan executed exactly as written; anticipated TRY401 pre-emption applied up front; `core.autocrlf=true` Windows host compensated by writing via `Path.write_bytes(content.encode("utf-8"))`); 0 new tests (06-02 lands tests); 5/5 end-to-end smoke checks PASS via inline `python -c`; 42/42 full suite PASS (zero regression — orchestrator/registry only ADD code) |
| Plan 06-02 duration | ~5 min (2026-05-02T04:38:18Z → 2026-05-02T04:43:11Z) — 1 task, 1 commit, 2 Rule-N deviations (Rule 1: replaced `capsys.err` with `caplog` in test 8 because pytest's logging plugin diverts records — supersedes plan's `propagate = False` recommendation; Rule 3: ruff I001 1-blank-line fix in import block); 8 new tests (FakeAdapter helper + 8 functions covering round-robin / GOAL ANCHOR / transcript-so-far / Keyword unanimity / continue-on-error / abort-on-error / return-Path / stdout+caplog discipline); 50/50 full suite PASS (42 prior + 8 new; zero regression) |
| Plan 07-01 duration | ~4 min (2026-05-02T05:04:31Z → 2026-05-02T05:09:09Z) — 2 tasks, 2 commits, 1 Rule-N deviation (Rule 3: `# noqa: RUF022` placement on multi-line `__all__` must be on the OPENING bracket line under ruff 0.15.8, not the closing bracket as the plan suggested); 4 files (2 new: `gemini.py` 94 lines + `codex.py` 100 lines mirroring `claude.py` with vendor-specific deltas only; 2 modified: `adapters/__init__.py` extended `__all__` from 3 to 5 entries + `registry.py` replaced `NotImplementedError` branch with concrete instantiation for `gemini`/`codex`); 50/50 full suite PASS (zero regression — new adapters not yet test-covered, that lands in 07-02); ADP-06, ADP-07 marked complete |
| Plan 07-02 duration | ~5 min (2026-05-02T05:19:32Z → 2026-05-02T05:24:33Z) — 2 tasks, 2 commits, **zero Rule-N deviations** (plan executed verbatim); 2 new test files (`tests/test_adapter_gemini.py` 261 lines + `tests/test_adapter_codex.py` 333 lines) mirroring `tests/test_adapter_claude.py` with vendor swaps + the headline `test_codex_empty_stdout_bug_regression` documenting Pitfall #2 / openai/codex#19945; 22 new tests (11 gemini + 11 codex, vs. plan's prose count of 10/10 — actual collected count is 11 each due to 5-entry / 4-entry parametrize blocks; all pass); 72/72 full suite PASS (50 prior + 22 new = 72; zero regression); zero source changes; LF-only + ASCII-only + zero direct `subprocess.run`/`Popen` in the new test files (use `fp` fixture + `monkeypatch.setattr(subprocess, "Popen", ...)` only); TST-05 lint test still 3/3 PASS; ADP-06 and ADP-07 now fully TEST-verified, closing Phase 7 |
| Total tests | 72 (8 config + 8 transcript + 7 adapters_base + 10 adapter_claude + 11 adapter_gemini + 11 adapter_codex + 3 subprocess_lint + 6 stop_conditions + 8 orchestrator) — was 50 before 07-02; +22 new tests (11 gemini + 11 codex) |

## Accumulated Context

### Key Decisions Locked in by Roadmap

1. **PyPI name `ultra-claude` reserved as `0.0.1` stub in Phase 1** — squat protection per Pitfall #5; verified available 2026-05-02.
2. **Adapter contract is a `typing.Protocol`, not an ABC** — third parties don't subclass; internal `_SubprocessAdapterMixin` absorbs duplication across the three bundled adapters.
3. **Subprocess invocation contract locked in Phase 4** — stdin-piped prompt (NOT `-p <prompt>` argv), `text=True`, `encoding="utf-8"`, `errors="replace"`, mandatory timeout, list-form argv, `shell=False`. Mitigates Pitfalls #1, #2, #3 simultaneously.
4. **Empty-stdout defense built into the mixin from Phase 4** — `returncode == 0` AND empty stdout raises `AdapterError`, defending against the active `codex exec` 0.124.0+ TTY bug (Pitfall #2) before `CodexAdapter` is even written.
5. **Cross-platform process-tree kill in Phase 4** — POSIX `os.killpg` + Windows `taskkill /T /F`; half-measures (timeout but no tree kill) leave orphaned children burning subscription quota.
6. **Stop-condition keyword matching uses anchored multiline regex + unanimity-window (N=2, M=2)** — naive substring matching causes false-positive consensus (sycophancy, Pitfall #4); anchored regex `^## Decision\nAGREED\s*$` requires structural agreement.
7. **Orchestrator is a single function, not a class** — promote to class only when v3 adds parallel speakers; YAGNI applied.
8. **Transcript is dual-format**: canonical markdown (re-promptable) + JSONL sidecar (parseable for v2 resume). Markdown uses non-markdown HTML-comment sentinels for turn delimiters to avoid markdown-in-markdown corruption (Pitfall #8).
9. **Continue-on-error by default** — adapter failures are logged + recorded as placeholder turns; run continues unless `abort_on_error: true` is set. A timed-out Codex shouldn't kill a 30-minute roundtable.
10. **`pip install` from PyPI is the v0.1.0 success criterion** — manual `python -m build` + `twine upload` for v1; auto-publish via Trusted Publishing deferred to v2.

### Open Todos (Phase-1 closure)

- [ ] **User action**: Run `python -m twine upload dist/ultra_claude-0.0.1*` per `.planning/phases/01-project-skeleton-pypi-name-reservation/PUBLISH.md` to close PKG-05. Artifacts and runbook are ready on the build host.
- [ ] After user reports "uploaded": run `pip install ultra-claude==0.0.1` from a fresh shell to confirm PyPI returns the stub; then mark PKG-05 complete in REQUIREMENTS.md.
- [ ] Verify each CLI's exact argv shape empirically at Phase 4 (Claude) and Phase 7 (Gemini, Codex) implementation time — vendor flag conventions may have drifted since research

### In-Phase-1 Progress

- [x] **Plan 01-01 (repository skeleton):** LICENSE (MIT, Freddy Chiu 2026), .gitignore, README.md stub with trademark disclaimer, CHANGELOG.md (Keep-a-Changelog), src/ultra_claude/__init__.py with `__version__ = "0.0.1"`. Commits: `562d05e`, `2b15b36`. Requirements: PKG-03, PKG-04, PKG-07.
- [x] **Plan 01-02 (pyproject.toml + tool configs):** PEP 621 metadata + hatchling >= 1.29 backend, dynamic version via `[tool.hatch.version] path = "src/ultra_claude/__init__.py"`, runtime deps pinned (click >= 8.3.3, pydantic >= 2.13.3, pyyaml >= 6.0.3), dev deps pinned (ruff >= 0.13, mypy >= 1.18, pytest >= 8.4 + auxiliaries), `[tool.hatch.build.targets.wheel] packages = ["src/ultra_claude"]` for src layout, ruff/mypy/pytest tool tables. NO `[project.scripts]` (CLI deferred to Phase 8). Commit: `b9bf3c5`. Requirements: PKG-02, PKG-07 (fully complete).
- [x] **Plan 01-03 (build + smoke test + PUBLISH.md):** Built `dist/ultra_claude-0.0.1.tar.gz` and `dist/ultra_claude-0.0.1-py3-none-any.whl` via `python -m build`; both pass `twine check`. Clean-venv smoke test verified `__version__ == importlib.metadata.version('ultra-claude') == "0.0.1"` (triple alignment) with `pip install -e ".[dev]"` succeeding in `.smoke-venv` (resolved click 8.3.3, pydantic 2.13.3, pyyaml 6.0.3, ruff 0.15.12, mypy 1.20.2, pytest 9.0.3, all dev tools callable). PUBLISH.md operator runbook authored (`e96ccb6`); `.smoke-venv/` added to .gitignore as defensive Rule 3 deviation (`3e31832`). Task 4 (twine upload) deferred to user action per orchestrator instruction. Commits: `3e31832`, `e96ccb6`. Requirements: PKG-05 staged-but-pending-user-action.

### Phase 2 Progress (COMPLETE)

- [x] **Plan 02-01 (`ConfigError` exception class):** New file `src/ultra_claude/exceptions.py` (1387 bytes, 34 lines, LF-only, ASCII-only). Defines `class ConfigError(Exception)` with a docstring documenting the three failure modes it wraps (`yaml.YAMLError`, `pydantic.ValidationError`, `FileNotFoundError`). Module docstring foreshadows Phase 4 `AdapterError`/`AdapterAuthError` additions. `__all__ = ["ConfigError"]` declared. Zero third-party imports. Commit: `ddfca71`. Requirements: CFG-03 partial (foundation completed by 02-02).
- [x] **Plan 02-02 (`config.py` + tests):** New file `src/ultra_claude/config.py` (9714 bytes, 267 lines, LF-only, ASCII-only) defining `AgentConfig` (Literal['claude','gemini','codex'] adapter, all required, min_length=1), `RoundtableConfig` (agents min_length=2, max_turns default 12 ge=2, stop_keywords default ['AGREED','DONE'], turn_order Literal['round_robin'], abort_on_error False, transcript_path optional, task optional), `load_config(path) -> RoundtableConfig`, `RoundtableConfig.from_yaml_string` classmethod, `format_validation_error(err, source_path) -> str` with specialised `literal_error`/`missing` wording, `_format_loc` ('agents', 0, 'adapter') -> 'agents[0].adapter', `_format_yaml_error` 1-indexed line/column. Both `BaseModel`s use `ConfigDict(extra='forbid')`. `__all__` exports 5 public symbols. Plus `tests/__init__.py` (0 bytes, package marker) and `tests/test_config.py` (8763 bytes, 276 lines, 8 test functions). All 8 tests pass; all 9 plan-level verification commands PASS. One Rule 3 deviation: ran `pip install -e ".[dev]"` (acknowledged in plan note) to make `ultra_claude` importable for pytest collection — no working-tree changes. Commits: `e97325a` (feat: add config schema and YAML loader), `5c272f0` (test: add config validation test suite). Requirements: CFG-01..CFG-05 all COMPLETE.

### Phase 4 Progress (COMPLETE — 3/3 plans)

- [x] **Plan 04-01 (extend exceptions.py + adapters/base.py):** New file `src/ultra_claude/adapters/__init__.py` (923 bytes, 21 lines, LF-only, ASCII-only) re-exports `Adapter` and `_SubprocessAdapterMixin`. New file `src/ultra_claude/adapters/base.py` (11805 bytes, 269 lines, LF-only, ASCII-only) defines `@runtime_checkable class Adapter(Protocol)` with `name: str` and `invoke(self, prompt: str, timeout: int) -> str`, plus `class _SubprocessAdapterMixin` with `cli_name: str` and `auth_error_markers: tuple[str, ...]` class-level annotations and a `_run_subprocess(argv, prompt, timeout) -> str` method that enforces the safe-subprocess contract end-to-end. The Popen call is split into per-platform `if os.name == "nt"` / `else` branches passing literal kwargs (`stdin/stdout/stderr=subprocess.PIPE`, `text=True`, `encoding="utf-8"`, `errors="replace"`, `shell=False`, plus either `creationflags=subprocess.CREATE_NEW_PROCESS_GROUP` or `start_new_session=True`) so mypy --strict resolves the `Popen[str]` overload directly. The prompt is piped via stdin via `proc.communicate(input=prompt, timeout=timeout)` (NEVER on argv — Pitfall #1 mitigation). On `subprocess.TimeoutExpired`, `_kill_process_tree(proc)` is called BEFORE re-raising as `AdapterError`; the helper dispatches by `os.name`: Windows runs `subprocess.run(["taskkill", "/T", "/F", "/PID", str(proc.pid)], ..., text=True, encoding="utf-8", errors="replace", shell=False, capture_output=True)` (the taskkill call itself respects the safe-contract kwargs so 04-03's TST-05 lint test will not flag it), POSIX runs `os.killpg(os.getpgid(proc.pid), signal.SIGKILL)` (with `# type: ignore[attr-defined]` scoped to two POSIX-only lines because mypy on Windows cannot see those attrs). On `FileNotFoundError` from Popen the mixin raises `AdapterAuthError` ("CLI not found on PATH; run `<cli> login`"). After communicate succeeds, the mixin checks (a) auth markers via case-insensitive substring match against `stdout + "\n" + stderr` -> `AdapterAuthError`, (b) `proc.returncode == 0 and not stdout.strip()` -> `AdapterError` naming `openai/codex#19945` (Pitfall #2 defense lifted into the mixin so EVERY adapter inherits it), (c) `proc.returncode != 0` -> `AdapterError` with stdout+stderr captured. Happy path returns `stdout.strip()`. Plus `src/ultra_claude/exceptions.py` extended (3569 bytes, 80 lines, LF-only, ASCII-only) with `class AdapterError(Exception)` and `class AdapterAuthError(AdapterError)` -- subclassing keeps continue-on-error semantics uniform; the module docstring tense was updated from "Phase 4 will append" to past tense; `__all__` extended to `["ConfigError", "AdapterError", "AdapterAuthError"]` with `# noqa: RUF022` and a justifying comment about the intentional Phase-2-then-Phase-4 ordering. All 5 plan-level verification commands PASS: imports work, mypy --strict on `src/ultra_claude/exceptions.py src/ultra_claude/adapters/` clean, ruff check on the same target clean, full pytest suite 16/16 PASS (zero regression), LF-only on disk on all 3 files. Plus 6 inline integration smoke checks (FileNotFoundError, empty-stdout, auth-marker, happy-path, non-zero exit, TimeoutExpired-with-kill) all PASS. Four Rule-3 deviations -- all about making the source acceptable to project's strict tooling without changing semantics: (1) refactored Popen call to per-platform branches so mypy resolves the right overload (the original `dict[str, object]` spread caused `call-overload` -> `Any` -> `no-any-return`); (2) added `# type: ignore[attr-defined]` on POSIX-only `os.getpgid`/`os.killpg` (mypy on Windows cannot see them); (3) replaced three `try/except/pass` with `contextlib.suppress(...)` for ruff SIM105; (4) added `# noqa: RUF022` + justifying comment for `__all__` order. Atomic commits: `e4423d0` (feat: add AdapterError and AdapterAuthError exceptions) + `eceb9da` (feat: add Adapter Protocol and _SubprocessAdapterMixin). SUMMARY at `.planning/phases/04-adapter-protocol-claudeadapter/04-01-SUMMARY.md`. Requirements: ADP-01, ADP-02, ADP-03, ADP-04, ADP-08 all COMPLETE; ADP-05 remains for plan 04-02; TST-05 remains for plan 04-03.
- [x] **Plan 04-02 (ClaudeAdapter):** New file `src/ultra_claude/adapters/claude.py` (4222 bytes, 95 lines, LF-only, ASCII-only) defines `class ClaudeAdapter(_SubprocessAdapterMixin)` with three class-level attributes (`name: str = "claude"`, `cli_name: str = "claude"`, `auth_error_markers: tuple[str, ...] = ("not logged in", "please run \`claude login\`", "please run /login", "authentication required", "authentication failed")` -- all lowercase; the mixin matches case-insensitively) and a single `invoke(self, prompt: str, timeout: int) -> str` method whose body is `argv = ["claude", "-p"]; return self._run_subprocess(argv, prompt, timeout)`. Zero direct subprocess imports; the mixin owns every subprocess call. `isinstance(ClaudeAdapter(), Adapter)` is True at runtime via the runtime_checkable Protocol's structural typing (the class declares both `name: str` and `invoke(prompt, timeout) -> str`). All four Phase 4 safety properties (UTF-8/replace decoding, mandatory timeout, process-tree kill, empty-stdout defense) flow through ClaudeAdapter without it touching subprocess directly -- the proof-of-concept that GeminiAdapter and CodexAdapter (Phase 7) will follow the same template line-for-line. Plus `src/ultra_claude/adapters/__init__.py` updated (1263 bytes, 26 lines, was 923 bytes / 21 lines): added `from .claude import ClaudeAdapter`; `__all__` extended to `["Adapter", "_SubprocessAdapterMixin", "ClaudeAdapter"]` with `# noqa: RUF022` plus justifying comment for chronological-by-introduction order; module docstring updated to mention ClaudeAdapter as landed (no longer "Phase 4 deliverable -- not yet shipped"). All 6 plan-level verification commands PASS: `from ultra_claude.adapters import ClaudeAdapter, Adapter` works and `isinstance(ClaudeAdapter(), Adapter)` is True; mypy --strict on `src/ultra_claude/adapters/` -> clean (3 files); ruff check on `src/ultra_claude/adapters/` -> clean; pytest 16/16 PASS (zero regression); LF-only on disk on both files; no `import subprocess` substring in `claude.py`. Plus the Task 1 inline assertion block (5 source-introspection asserts) all PASS. Two Rule-3 deviations: (1) docstring on claude.py rephrased to avoid the literal `import subprocess` substring -- the plan's verification command checks `'import subprocess' not in src` (substring, not AST), so descriptive prose containing the literal phrase tripped the assertion; replaced with semantically equivalent wording ("This file deliberately does NOT pull in the stdlib subprocess module"); (2) ruff RUF022 on `__all__` order in `adapters/__init__.py` -- the plan acceptance criteria demand `["Adapter", "_SubprocessAdapterMixin", "ClaudeAdapter"]` in chronological-by-introduction order (matching `exceptions.py` from 04-01), so added `# noqa: RUF022` plus justifying comment. Atomic commits: `85e1c8f` (feat(04-02): add ClaudeAdapter wrapping `claude -p` via _SubprocessAdapterMixin) + `40dd2ab` (feat(04-02): re-export ClaudeAdapter from adapters package). SUMMARY at `.planning/phases/04-adapter-protocol-claudeadapter/04-02-SUMMARY.md`. Requirements: ADP-05 COMPLETE; TST-05 remains for plan 04-03.
- [x] **Plan 04-03 (adapter tests + TST-05 lint tripwire):** Three new test files closed Phase 4 by verifying every locked decision in 04-CONTEXT.md with executable tests. (1) `tests/test_adapters_base.py` (5043 bytes, 122 lines, LF-only, ASCII-only) — 7 tests covering ADP-01 (runtime_checkable Protocol via `_is_runtime_protocol` dunder; structural subtyping with positive `ClaudeAdapter` + duck-typed `ThirdPartyAdapter` cases; negative `HalfBaked` case lacking `invoke`) and ADP-02 partial (mixin annotations, `ClaudeAdapter` inheritance from `_SubprocessAdapterMixin`, populated `cli_name` + non-empty `auth_error_markers` tuple). (2) `tests/test_adapter_claude.py` (9655 bytes, 253 lines, LF-only, ASCII-only) — 10 tests via `pytest-subprocess` `fp` fixture covering all five behaviour paths: 2 happy paths (one asserting trimmed stdout return + `stdin_callable` capturing the piped prompt to prove Pitfall #1 mitigation; another asserting `fp.calls` records exact list-form argv `["claude","-p"]`); 2 empty-stdout variants (literal `""` AND whitespace-only `"   \n\t\n  "` proving the defense matches `stdout.strip() == ""`); 1 FileNotFoundError test using `monkeypatch.setattr(subprocess, "Popen", _raise_fnf)` -> `AdapterAuthError` with login/install/path hint; 4 parametrized auth-marker variants (`"Error: not logged in"`, `"ERROR: NOT LOGGED IN"`, `"Authentication required to continue"`, `"please run /login first"` — all match case-insensitively); 1 TimeoutExpired test using `fp.register(callback=raise_timeout)` + `monkeypatch.setattr(Mixin, "_kill_process_tree", staticmethod(fake_kill))` to record the kill-helper invocation, then asserting `len(kill_calls) >= 1` AND the re-raised `AdapterError` message contains "claude" + "tim". (3) `tests/test_subprocess_lint.py` (7853 bytes, 200 lines, LF-only, ASCII-only) — 3 tests implementing TST-05: `_python_files()` rglobs `src/ultra_claude/`, `_is_subprocess_call(node)` detects both `subprocess.run`/`subprocess.Popen` attribute access AND bare-imported `run`/`Popen`, `_enumerate_subprocess_calls()` walks all .py files via `ast.parse` + `ast.walk`, and the headline test aggregates ALL violations into a single multi-line `pytest.fail(...)` listing every `file:lineno`. Manual paranoia check confirmed the lint test FIRES: injected `src/ultra_claude/_scratch.py` with `subprocess.run(["echo","hi"])` (no safe kwargs) -> test exits 1 with file:lineno violation report; second variant with `shell=True` -> test exits 1 with "shell=True is forbidden" branch firing; scratch file deleted in both cases. Total project test count: **16 -> 36** (8 config + 8 transcript + 7 adapters_base + 10 adapter_claude + 3 subprocess_lint; zero regression in the 16 prior tests). All plan-level verification commands PASS: `pytest tests/` 36/36; `mypy --strict src/ultra_claude` clean (7 files); `ruff check` clean on the 3 new test files AND on `src/ultra_claude/adapters`; LF-only + ASCII-only on disk on all 3 files. Two Rule-3 deviations: (1) ruff SIM102 on nested-if in `_is_subprocess_call` -> combined into single `if isinstance(...) and ...: return True` (no semantic change); (2) ruff I001 on `tests/test_adapters_base.py` -> auto-fixed by `ruff --fix` (blank-line normalization). Out-of-scope: 4 pre-existing ruff errors in Phase 2 files (`src/ultra_claude/config.py`: RUF022 + UP037; `tests/test_config.py`: I001 + F401) logged at `.planning/phases/04-adapter-protocol-claudeadapter/deferred-items.md` for a future chore plan — confirmed pre-existing via `git log --oneline -- <path>` (commits `e97325a` + `5c272f0` from Phase 2). Atomic commits: `ab17d77` (test: Adapter Protocol structural typing) + `e0ea60e` (test: ClaudeAdapter behaviour via pytest-subprocess fp fixture) + `e16c4f9` (test: TST-05 ast-walking subprocess-contract lint test). SUMMARY at `.planning/phases/04-adapter-protocol-claudeadapter/04-03-SUMMARY.md`. Requirements: TST-05 COMPLETE; ADP-01..05 + ADP-08 verified by executable tests. Phase 4 fully closed (3/3 plans, 7/7 ADP+TST requirements).

### Phase 5 Progress (COMPLETE — 1/1 plan)

- [x] **Plan 05-01 (`stop_conditions.py` + tests):** New file `src/ultra_claude/stop_conditions.py` (7142 bytes, 181 lines, LF-only, ASCII-only) defining `@runtime_checkable class StopCondition(Protocol)` with `check(self, transcript: Transcript) -> bool`, plus three concrete strategies: `class Keyword(keywords, *, n=2, m=2)` pre-compiles each keyword to `re.compile(rf"^{re.escape(kw)}\s*$", re.MULTILINE)` at construction (Pitfall #4 mitigation locked at line 105), inspects only the last `n` turns from `transcript.read_turns()`, counts distinct `.agent` strings among matching turns, returns True iff `>= m` (single agent saying AGREED twice -> False because 1 < 2; two distinct agents both AGREED -> True); `class MaxTurns(max_turns)` returns `len(transcript) >= self._max_turns` (exact-boundary equality); `class AnyOf(conditions)` uses lazy `any(c.check(transcript) for c in self._conditions)` for short-circuit composition. Module imports only `re` + `typing.Protocol/runtime_checkable` from stdlib + `from .transcript import Transcript`; zero subprocess; pure logic. `__all__ = ["StopCondition", "Keyword", "MaxTurns", "AnyOf"]` with `# noqa: RUF022` chronological-by-introduction comment matching `adapters/__init__.py` and `exceptions.py` conventions. Plus `tests/test_stop_conditions.py` (7347 bytes, 208 lines, LF-only, ASCII-only, 6 tests, all PASS): `test_stop_condition_protocol_structural` (duck-typed `FakeStop` positive, `HalfBaked` negative, all 3 bundled classes are StopCondition instances — STP-01), `test_keyword_anchored_regex_rejects_substring` ("I am NOT going to say AGREED yet" with m=1 isolation -> False — STP-02 + Pitfall #4), `test_keyword_unanimity_two_agents_two_turns` (Architect+Critic both `## Decision\nAGREED` -> True with defaults — STP-03 happy path), `test_keyword_single_agent_self_stop_blocked` (Architect twice both ending AGREED -> False — STP-03 voting-itself-off-the-island defense), `test_max_turns_equality` (11 turns False, 12 turns True for `MaxTurns(12)` — STP-04 boundary), `test_anyof_short_circuit` (`AnyOf([MaxTurns(3), Keyword(["AGREED"])])` on 3-turn AGREED-free transcript -> True; loose `MaxTurns(99)` -> False sanity — STP-05 + lazy `any` correctness). Tests use real `Transcript` writes on `tmp_path` (NOT mocks) so unanimity-window logic is exercised against actual JSONL sidecar reads. Total project test count: **36 -> 42** (zero regression). All end-of-phase verification gates pass: `pytest tests/` 42/42 PASS; `mypy --strict src/ultra_claude` clean (8 files; was 7); `ruff check` clean on the 2 new files; LF-only + ASCII-only on disk; runtime_checkable Protocol smoke test PASSES; TST-05 lint test still passes (free regression check — stop_conditions.py adds zero subprocess calls). Zero Rule-N deviations during execution: plan was followed exactly as written. The anticipated `# noqa: RUF022` annotation on `__all__` was applied as documented in the plan — not a deviation. Commits: `e56a779` (feat(05-01): add StopCondition Protocol + Keyword + MaxTurns + AnyOf), `9dbc164` (test(05-01): add stop_conditions test suite covering STP-01..STP-05 + Pitfall #4). SUMMARY at `.planning/phases/05-stop-conditions/05-01-SUMMARY.md`. Requirements: STP-01..STP-05 all COMPLETE.

### Phase 6 Progress (COMPLETE — 2/2 plans)

- [x] **Plan 06-01 (`registry.py` + `orchestrator.py`):** Two new files compose Phases 3+4+5 into the `run(config, task) -> Path` entry point. (1) `src/ultra_claude/registry.py` (2286 bytes, 56 lines, LF-only, ASCII-only) defines `get_adapter(adapter_kind: str) -> Adapter` stateless dispatcher: `"claude"` -> `ClaudeAdapter()`; `"gemini"` / `"codex"` -> `NotImplementedError` whose message names "Phase 7 (next phase)"; any other string -> `ValueError` (defence-in-depth vs `AgentConfig.adapter` Literal). Function-not-dict so a missing CLI does not blow up at import time; function-not-class because dispatch is stateless. `__all__ = ["get_adapter"]`. Single import `from .adapters import Adapter, ClaudeAdapter`. Zero subprocess imports. (2) `src/ultra_claude/orchestrator.py` (11876 bytes, 307 lines, LF-only, ASCII-only) exposes `run(config: RoundtableConfig, task: str, *, transcript_path: Path | None = None, adapter_factory: Callable[[str], Adapter] | None = None) -> Path` plus four private helpers: `_logger`, `_DEFAULT_TRANSCRIPT_PATH = Path("ultra-claude-transcript.md")`, `_DEFAULT_TIMEOUT_SECONDS = 120`, `_ensure_default_handler()` (idempotent stderr StreamHandler attach), `_build_prompt(*, task, agent, transcript_so_far) -> str` (assembles `# Task` -> `# Your role` -> transcript-so-far (omitted when empty on turn 1) -> GOAL ANCHOR `# Reminder of the task` + `Respond now as {name} ({role}). Stay focused on the task above.` per ORC-03 / Pitfall #6). Body resolves transcript_path (kwarg > config.transcript_path > default), `mkdir(parents=True, exist_ok=True)` parent BEFORE `Transcript()` (D-11 compliance), builds adapter list via `factory = adapter_factory if adapter_factory is not None else get_adapter` ONCE up front (NotImplementedError halts before any transcript is written), wires `composite = AnyOf([MaxTurns(config.max_turns), Keyword(config.stop_keywords)])`, then loops `for turn_idx in range(1, config.max_turns + 1)` with round-robin `agent_cfg = config.agents[(turn_idx - 1) % n_agents]`, try/except/else around `adapter.invoke(prompt, _DEFAULT_TIMEOUT_SECONDS)`: on `AdapterError as exc` (covers `AdapterAuthError` subclass) calls `_logger.exception("turn %d: agent %s failed", turn_idx, agent_cfg.name)` (no trailing exc arg — TRY401 pre-empted), if `config.abort_on_error` re-raises via bare `raise`, else appends placeholder `"[adapter error: {exc}]"` turn; on success appends real turn + INFO log. After every append, `if composite.check(transcript): break`. Returns `transcript.markdown_path`. Function-not-class per CLAUDE.md "no class without state" rule. All plan-level verification gates PASS: `pytest tests/` 42/42 (zero regression — orchestrator and registry only ADD code, do not modify any existing files); `mypy --strict src/ultra_claude` clean (10 modules; was 8 — added registry.py and orchestrator.py); `ruff check src/ultra_claude/registry.py src/ultra_claude/orchestrator.py` clean (zero violations on the two new files); LF-only + ASCII-only on disk on both new files (registry: 0 CRLF / 56 LF; orchestrator: 0 CRLF / 307 LF; staged blobs are also LF-only despite `core.autocrlf=true` host because we wrote via `Path.write_bytes(content.encode("utf-8"))` which dodges Git's working-tree filter); TST-05 lint test still passes (free regression check — orchestrator/registry add zero subprocess calls). 5/5 end-to-end smoke checks PASS via inline `python -c` script (NOT committed — `tests/test_orchestrator.py` lands in 06-02): SMOKE 1 = 3 fake agents max_turns=6 → 6 turns in `[A1, A2, A3, A1, A2, A3]` round-robin order with first prompt containing `# Task\n\nSmoke task` AND `# Reminder of the task\n\nSmoke task` AND `Respond now as A1 (r1). Stay focused on the task above.` AND turn-4 prompt containing all three earlier turn outputs (transcript-so-far reaches turn N+1 correctly); SMOKE 2 = continue-on-error with BoomAdapter (raises `AdapterAuthError("not logged in")`) + GoodAdapter → all 4 turns landed; placeholder `[adapter error: not logged in]` for Boomer's turns; run reached max_turns; SMOKE 3 = `abort_on_error=True` with same setup → `AdapterAuthError` propagated out of `run()` (subclass-aware single-clause catch + bare `raise`); SMOKE 4 = both agents output `"I'm done.\nAGREED\n"` max_turns=10 → stopped at turn 2 (Keyword unanimity-window n=2/m=2 satisfied); SMOKE 5 = no `transcript_path` kwarg + no `config.transcript_path` → resolved to `Path("ultra-claude-transcript.md")` in cwd. Zero Rule-N deviations during execution: plan executed exactly as written; both anticipated adjustments (TRY401 pre-emption + `core.autocrlf=true` Windows compensation) were called out in Task 1/Task 2 notes and applied as documented — not Rule-N deviations. Out-of-scope (NOT actioned): whole-package `ruff check src/ultra_claude` still surfaces 2 pre-existing errors in `src/ultra_claude/config.py` (RUF022 + UP037 from commit `e97325a` Phase 2; already logged at `.planning/phases/04-adapter-protocol-claudeadapter/deferred-items.md`); per scope-boundary rule, only auto-fix issues caused by current task's changes; the plan's done criterion explicitly required only "ruff check clean on the two new files" — both pass. Atomic commits: `8cfee40` (feat(06-01): add adapter registry dispatcher) + `b9b80b3` (feat(06-01): add orchestrator run() function). SUMMARY at `.planning/phases/06-orchestrator-loop/06-01-SUMMARY.md`. Requirements: ORC-01..ORC-06 satisfied at IMPLEMENTATION level; executable test verification (`tests/test_orchestrator.py` with FakeAdapter helper covering all 8 06-CONTEXT.md test cases) lands in plan 06-02 — boxes remain unchecked in REQUIREMENTS.md until 06-02 closes.
- [x] **Plan 06-02 (orchestrator tests):** New file `tests/test_orchestrator.py` (17306 bytes, 460 lines, LF-only, ASCII-only) defining `class FakeAdapter` (40-line pure-Python Protocol satisfier with `name`/`canned_output`/`raise_exc`/`calls` fields and `invoke(prompt, timeout) -> str` recording every call), `_agent()` helper for AgentConfig fixtures (always `adapter='claude'` since the registry is bypassed by `adapter_factory`), `_make_factory(adapters: dict[str, FakeAdapter]) -> Callable[[str], Adapter]` dispenser using dict insertion order (mirrors how 06-01's `run()` calls factory once per agent in declared order), plus 8 test functions covering every ORC-01..ORC-06 path: (1) `test_run_3_agent_max_turns_6_writes_6_turns` (ORC-01+ORC-02 — 3 agents + max_turns=6 -> 6 turns in `[alpha, beta, gamma, alpha, beta, gamma]` declared order; each FakeAdapter invoked exactly twice; result Path equals input); (2) `test_run_includes_task_in_prompt` (ORC-03 — first prompt contains `# Task` header AND `# Reminder of the task` footer with `Respond now as alpha` + `Stay focused on the task above`; task appears >=2 times; system_prompt is in prompt); (3) `test_run_includes_transcript_so_far` (ORC-03 — turn 3 prompt to alpha contains BOTH `ALPHA-FIRST-OUTPUT` and `BETA-FIRST-OUTPUT`); (4) `test_run_stops_on_keyword_unanimity` (ORC-04 — 3 agents all returning `"AGREED"` with default n=2/m=2 halts after turn 2 with max_turns=6; gamma never invoked); (5) `test_run_continues_on_adapter_error` (ORC-05 — beta's AdapterError gets logged + placeholder turn `"[adapter error: simulated CLI failure]"` appended; all 3 turns landed; gamma still invoked); (6) `test_run_aborts_on_error_when_configured` (ORC-05 — abort_on_error=True with same setup -> `pytest.raises(AdapterError, match="simulated abort")`; gamma NOT invoked); (7) `test_run_returns_transcript_path` (ORC-01 — `isinstance(result, Path)` + result.exists() + 4 sentinel comments `<!-- turn:N agent: -->` for N in 1..4); (8) `test_run_logs_progress_to_stderr_only` (ORC-06 — `capsys.readouterr().out == ""` for stdout discipline AND `caplog.text` contains "starting roundtable" + "turn 1 starting" + "turn 2 starting" AND >= 3 records from `ultra_claude.orchestrator` logger name). All 8 tests use the `adapter_factory` injection seam — zero subprocess launches anywhere in this test file. **Total project test count: 42 -> 50** (zero regression). All plan-level verification gates PASS: `pytest tests/test_orchestrator.py -x -v` 8/8 PASS; `pytest tests/` 50/50 PASS; `ruff check tests/test_orchestrator.py` clean; `mypy --strict src/ultra_claude` clean (10 source files — unchanged); TST-05 lint test still passes; LF-only on disk (0 CRLF / 460 LF); ASCII-only on disk; staged blob LF-only (17306 bytes / 0 CRLF / 460 LF) despite `core.autocrlf=true`. **Two Rule-N deviations during execution:** (1) **Rule 1 - Bug**: test 8 initially failed with `assert 'starting roundtable' in '' (where '' = CaptureResult(out='', err='').err)` — diagnosed pytest's built-in logging plugin diverts records away from the StreamHandler's stderr writes. Plan's verification note suggested `propagate = False`, but that fix wouldn't help (the issue is plugin diversion, not propagation). **Replaced** `capsys.err` assertions with `caplog: pytest.LogCaptureFixture` + `caplog.set_level(logging.INFO, logger="ultra_claude.orchestrator")` + `assert "..." in caplog.text` + `len(orchestrator_records) >= 3`. Kept `assert captured.out == ""` for stdout discipline (capsys IS the right tool there). This is the pytest-canonical pattern for asserting logging output and supersedes the plan's recommendation. (2) **Rule 3 - Blocking**: ruff I001 ("Import block is un-sorted or un-formatted") — required removing one blank line between imports and the first `# ---` comment block. No semantic change. Both fixes were applied before commit, so commit `747f003` already reflects the corrected form. Atomic commit: `747f003` (test(06-02): add orchestrator test suite covering ORC-01..ORC-06). SUMMARY at `.planning/phases/06-orchestrator-loop/06-02-SUMMARY.md`. Requirements: **ORC-01..ORC-06 all COMPLETE** (boxes checked in REQUIREMENTS.md). Phase 6 fully CLOSED (2/2 plans, 6/6 ORC requirements). Phase 7 (Gemini + Codex Adapters) is unblocked.

### Phase 7 Progress (COMPLETE — 2/2 plans)

- [x] **Plan 07-01 (gemini.py + codex.py + adapters/__init__.py + registry.py wiring):** New file `src/ultra_claude/adapters/gemini.py` (4123 bytes, 94 lines, LF-only, ASCII-only) defines `class GeminiAdapter(_SubprocessAdapterMixin)` with `name: str = "gemini"`, `cli_name: str = "gemini"`, 4-marker `auth_error_markers` tuple (`"not logged in"`, `` "please run `gemini auth login`" ``, `"authentication required"`, `"no credentials"` — kept DISTINCT from Claude/Codex per locked decision D-02 because real-world auth-failure phrasing varies by CLI), and a one-line `invoke(self, prompt: str, timeout: int) -> str` whose body is `argv = ["gemini", "-p"]; return self._run_subprocess(argv, prompt, timeout)`. Zero direct subprocess imports — the mixin is the only sanctioned subprocess site. New file `src/ultra_claude/adapters/codex.py` (4339 bytes, 100 lines, LF-only, ASCII-only) defines `class CodexAdapter(_SubprocessAdapterMixin)` with `name: str = "codex"`, `cli_name: str = "codex"`, 3-marker `auth_error_markers` tuple (`"not logged in"`, `` "please run `codex login`" ``, `"authentication required"`), and a one-line `invoke()` whose body is `argv = ["codex", "exec"]; return self._run_subprocess(argv, prompt, timeout)`. Module docstring contains the literal `openai/codex#19945` reference per locked decision D-03 — documenting why the inherited Pitfall #2 empty-stdout defense (built into the mixin in Phase 4) matters specifically for this adapter; NO Codex-specific defensive code added because the Phase 4 mixin already catches the regression for every adapter. Zero direct subprocess imports. Both adapters satisfy `isinstance(GeminiAdapter(), Adapter)` and `isinstance(CodexAdapter(), Adapter)` via the runtime_checkable Protocol's structural typing. `src/ultra_claude/adapters/__init__.py` updated (1798 bytes, 40 lines, LF-only, ASCII-only — was 1263 bytes / 26 lines): added `from .codex import CodexAdapter` and `from .gemini import GeminiAdapter`; `__all__` extended from 3 entries to 5 (`["Adapter", "_SubprocessAdapterMixin", "ClaudeAdapter", "GeminiAdapter", "CodexAdapter"]`) with `# noqa: RUF022` on the OPENING bracket line `__all__ = [  # noqa: RUF022` (NOT closing bracket — see Rule 3 deviation below) plus a justifying multi-line comment about preserving roadmap-introduction order; module docstring updated to describe all three concrete adapters. `src/ultra_claude/registry.py` updated (1998 bytes, 51 lines, LF-only, ASCII-only — was 2286 bytes / 56 lines): import line is now `from .adapters import Adapter, ClaudeAdapter, CodexAdapter, GeminiAdapter` (alphabetical-by-name as ruff `I` rule enforces); the `if adapter_kind in ("gemini", "codex"): raise NotImplementedError(...)` branch was REMOVED entirely and replaced with two explicit branches `if adapter_kind == "gemini": return GeminiAdapter()` / `if adapter_kind == "codex": return CodexAdapter()`; module docstring tense updated from forward-looking ("Phase 7 will extend...") to present/past ("After Phase 7 ... all three adapter literals return real instances; the NotImplementedError branch was removed when GeminiAdapter and CodexAdapter landed"); Raises: section drops `NotImplementedError`, only `ValueError` remains. All plan-level verification gates PASS: headline structural / registry check (asserting `isinstance(GeminiAdapter(), Adapter)` AND `isinstance(CodexAdapter(), Adapter)` AND `issubclass(GeminiAdapter, _SubprocessAdapterMixin)` AND `issubclass(CodexAdapter, _SubprocessAdapterMixin)` AND `isinstance(get_adapter('gemini'), GeminiAdapter)` AND `isinstance(get_adapter('codex'), CodexAdapter)` AND `isinstance(get_adapter('claude'), ClaudeAdapter)` AND `get_adapter('not-a-real-adapter')` raises ValueError) prints `OK: structural / registry checks pass`; `pytest tests/` 50/50 PASS (zero regression — the new adapters are not yet test-covered, that lands in plan 07-02); `mypy --strict src/ultra_claude` clean (12 source files = 10 prior + gemini.py + codex.py); `ruff check` clean on the 4 modified/new files; `pytest tests/test_subprocess_lint.py` 3/3 PASS (TST-05 lint catches no new violations because gemini.py and codex.py contain zero `import subprocess` / `from subprocess` references); LF-only + ASCII-only on disk on all 4 files (verified via `b"\r\n" not in path.read_bytes()` and `all(ord(c) < 128 for c in path.read_text(encoding="utf-8"))`); staged blobs LF-only despite `core.autocrlf=true` Windows host (verified via `git cat-file blob`: gemini.py 94 LF / 0 CRLF, codex.py 100 LF / 0 CRLF, __init__.py 40 LF / 0 CRLF, registry.py 51 LF / 0 CRLF); the literal string `openai/codex#19945` is present in codex.py module docstring (D-03 traceability); exactly 4 working-tree changes after the plan completes (2 new + 2 modified) — matches plan success criterion exactly. **One Rule-N deviation during execution:** **Rule 3 - Blocking**: under ruff 0.15.8, the plan-suggested closing-bracket placement of `# noqa: RUF022` on a multi-line `__all__` (`]  # noqa: RUF022`) tripped TWO errors simultaneously: (a) **RUF022** "`__all__` is not sorted" because ruff didn't see the noqa as covering the violation site (which ruff anchors at the opening bracket line where `__all__ = [` begins); (b) **RUF100** "Unused `noqa` directive (unused: `RUF022`)" because ruff considered the closing-bracket noqa orphan. The plan's reference example for `exceptions.py` works because that `__all__` is single-line, so the noqa naturally lives on the same line as the violation. Multi-line `__all__` requires the noqa on the OPENING bracket line. **Fix**: Moved `# noqa: RUF022` from the closing bracket line `]  # noqa: RUF022` to the opening bracket line `__all__ = [  # noqa: RUF022`. Single-character relocation; ordering of items unchanged; intent preserved. After the fix, ruff cleared cleanly; the corrected form is what landed in commit `5f067c1`. **Out-of-scope (NOT actioned):** 4 pre-existing ruff errors carried forward from Phases 2/4 (`src/ultra_claude/config.py:38` RUF022; `src/ultra_claude/config.py:110` UP037; `tests/test_config.py:12` I001; `tests/test_config.py:24` F401). Confirmed pre-existing via `git stash` round-trip — same 4 errors with and without plan 07-01 changes. Logged at `.planning/phases/07-gemini-codex-adapters/deferred-items.md` for a future cleanup pass (likely Phase 9 quality-bar). Atomic commits: `4a09f27` (feat(07-01): add GeminiAdapter and CodexAdapter — 2 files changed, 194 insertions(+)) + `5f067c1` (feat(07-01): wire Gemini/Codex into adapters package and registry — 2 files changed, 28 insertions(+), 19 deletions(-)). SUMMARY at `.planning/phases/07-gemini-codex-adapters/07-01-SUMMARY.md`. Requirements: ADP-06, ADP-07 marked complete in REQUIREMENTS.md (implementation level — test verification by plan 07-02 closes the loop).
- [x] **Plan 07-02 (test suites for gemini + codex):** New file `tests/test_adapter_gemini.py` (10132 bytes, 261 lines, LF-only, ASCII-only) defines 6 test functions (11 collected with the 5-entry auth-marker parametrize): happy path with `stdin_callable` capture proving prompt is piped via stdin not argv (Pitfall #1 mitigation), list-form `["gemini", "-p"]` argv defensively asserted via `fp.calls`, empty-stdout-with-rc-0 -> AdapterError naming "gemini" + referencing empty/19945 (Pitfall #2 inherited mixin defense), whitespace-only stdout same defense, FileNotFoundError -> AdapterAuthError with re-auth/install hint (ADP-08 path 1), parametrized auth-marker substring (5 cases: lowercase exact / uppercase / different marker / buried marker / vendor-specific marker with backticks `please run \`gemini auth login\``) -> AdapterAuthError (ADP-08 path 2 — case-insensitive matching + backticks-survive-substring), TimeoutExpired -> `_kill_process_tree` recorded via monkeypatch + AdapterError naming "gemini" + "tim" (Pitfall #5 / ADP-04). New file `tests/test_adapter_codex.py` (13478 bytes, 333 lines, LF-only, ASCII-only) mirrors gemini suite with vendor swaps to `["codex", "exec"]` argv and 4-entry Codex auth-marker parametrize (3 markers + 1 case variant). The headline `test_codex_empty_stdout_bug_regression` at line 129 explicitly documents Pitfall #2 / openai/codex#19945 with a ~30-line docstring covering: (a) bug source URL `https://github.com/openai/codex/issues/19945`, (b) bug shape pinned via `fp.register(["codex", "exec"], stdout="", stderr="warning: no TTY attached; using non-interactive mode", returncode=0)`, (c) defense location named (`_SubprocessAdapterMixin._run_subprocess` from Phase 4 plan 04-01), (d) explicit "what the test pins vs deliberately does NOT pin" section so the test is decoupled from upstream bug-fix lifecycle. The headline regression test passes WITHOUT any Codex-specific defensive code in `CodexAdapter` -- the inherited Phase 4 mixin's `if proc.returncode == 0 and not stdout.strip(): raise AdapterError(...)` line catches the bug shape directly. Two assertion anchors: (1) `"codex" in msg.lower()`, (2) `"empty" in msg.lower() or "19945" in msg`. Both files exclusively use the `pytest-subprocess` `fp` fixture (happy paths, empty-stdout, auth markers, timeout) and `monkeypatch.setattr(subprocess, "Popen", ...)` (FileNotFoundError simulation only) -- ZERO real CLI launches, ZERO direct `subprocess.run`/`Popen` calls, both files independently runnable in isolation. **Total project test count: 50 -> 72** (50 prior + 11 gemini + 11 codex = 72; +22 new tests; zero regression). All 13 plan-level verification gates PASS: `pytest tests/test_adapter_gemini.py -x -v` 11/11 PASS; `pytest tests/test_adapter_codex.py -x -v` 11/11 PASS; `pytest tests/test_adapter_codex.py::test_codex_empty_stdout_bug_regression -x -v` 1/1 PASS (the headline regression); `pytest tests/` 72/72 PASS; `ruff check tests/test_adapter_gemini.py tests/test_adapter_codex.py` clean; `pytest tests/test_subprocess_lint.py -x` 3/3 PASS (TST-05 lint catches no new violations); LF-only + ASCII-only on disk on both new files; staged blobs LF-only despite `core.autocrlf=true` (gemini blob 10132 bytes / 0 CRLF / 261 LF; codex blob 13478 bytes / 0 CRLF / 333 LF); `def test_codex_empty_stdout_bug_regression` present at line 129; `openai/codex#19945` literal present 3 times in codex test file (module docstring + comment block + regression-test docstring URL); `from __future__ import annotations` in both files. **Zero Rule-N deviations** during execution -- plan executed verbatim. The plan's English prose said "10 collected tests total" for each file but the actual file as specified produces 11 each (the prose miscounted by 1; parametrize blocks have 5 / 4 entries which when added to 5 / 6 standalone functions sum to 11, not 10). The truth-list count of FUNCTIONS (6 / 7) is correct. Atomic commits: `4377a27` (test(07-02): add GeminiAdapter test suite covering ADP-06) + `e538e88` (test(07-02): add CodexAdapter test suite covering ADP-07 and openai/codex#19945 regression). SUMMARY at `.planning/phases/07-gemini-codex-adapters/07-02-SUMMARY.md`. **ADP-06 and ADP-07 now both IMPLEMENTATION-verified (07-01) AND TEST-verified (07-02), closing Phase 7 fully.** Phase 8 (CLI Surface) is now unblocked.

### Phase 3 Progress (COMPLETE)

- [x] **Plan 03-01 (`transcript.py` + tests):** New file `src/ultra_claude/transcript.py` (11708 bytes, 295 lines, LF-only, UTF-8) defining `TurnRecord(BaseModel)` (Pydantic v2; `turn` ge=1, `agent`/`role` min_length=1, `prompt_hash` exactly 64 hex chars, `output` str; ConfigDict extra=forbid) and `Transcript` class with `__init__(markdown_path, *, header_task=None, started_at=None)`, `markdown_path`/`jsonl_path` read-only `@property` (jsonl path = markdown path with `.jsonl` appended via `with_suffix(suffix + ".jsonl")` — literal append, not replace), `append_turn(turn, agent, role, prompt, output)` (writes BOTH markdown block + JSONL line; SHA-256 hex prompt_hash on UTF-8-encoded prompt; both opens use `mode="a", encoding="utf-8", newline="\n"`; header rendered on first write only when stat().st_size==0), `read_turns()` (returns `[]` if sidecar missing, else parses each line via `TurnRecord.model_validate_json`), `__len__`, `markdown_text()`/`jsonl_text()` read-back helpers. Sentinel format: `<!-- turn:N agent:Name -->` (Pitfall #8 mitigation; locked for Phase 5 stop conditions). `__init__` raises `OSError` when parent directory missing (D-11 — no auto-mkdir); does NOT erase existing markdown (D-10 — idempotent re-open). Plus `tests/test_transcript.py` (12880 bytes, 337 lines, 8 tests, all PASS): `test_three_turn_round_trip_appends_to_markdown` (TRX-01), `test_each_turn_has_html_comment_sentinel` (TRX-02), `test_jsonl_sidecar_records_match_schema` (TRX-03), `test_lf_only_on_disk` (TRX-04), `test_utf8_round_trip` (TRX-05 — em-dash + smart quotes + rocket emoji), `test_read_turns_returns_empty_list_when_sidecar_missing` (D-08), `test_init_raises_oserror_when_parent_missing` (D-11), `test_idempotent_creation_does_not_erase_existing_markdown` (D-10). Full suite: 16/16 PASS (8 Phase 2 + 8 Phase 3 — zero regression). Two deviations: Rule 1 (Python 3.11 `SyntaxError: f-string expression part cannot include a backslash` in `test_lf_only_on_disk` — fixed by lifting CRLF byte literal counts into locals before f-string interpolation; behavior identical) + Rule 3 (ruff lint cleanups: I001 extra blank line removed, B905 added `strict=True` to `zip()`, RUF001 x3 noqa on intentional smart-quote test fixtures). Commits: `88b6186` (feat: add transcript module with TurnRecord + Transcript classes), `6230667` (test: add transcript test suite covering TRX-01..TRX-05). SUMMARY at `.planning/phases/03-transcript-module/03-01-SUMMARY.md`. Requirements: TRX-01..TRX-05 all COMPLETE.

### Out-of-Scope Discoveries Logged (not actioned in 02-01)

- **`core.autocrlf=true` on Windows host risks CRLF on checkout.** Working-tree and git index are LF-only after this commit, but a future clone/checkout on Windows could materialise CRLF, breaking the cross-platform discipline (CLAUDE.md constraint #6). Recommended fix is a repo-root `.gitattributes` forcing LF. Logged at `.planning/phases/02-config-schema-yaml-loader/deferred-items.md`. Project-wide concern, not specific to plan 02-01 — should be addressed via a small chore plan.

### Active Blockers

None for autonomous execution. Phase 1 final closure (PKG-05) gates on a one-step user action: running `twine upload`. Subsequent phases (2+) proceed independently of PyPI publish state — they consume the local pyproject.toml + src/ultra_claude/, both of which are committed.

### Research Flags Carried Forward

| Phase | Verification Required at Plan Time |
|-------|-----------------------------------|
| Phase 4 | `claude -p` exact current argv shape and stdin-acceptance behaviour (verify empirically) |
| Phase 7 | `gemini -p` non-interactive flag (issue #19774); `codex exec` `--quiet`/stdin support |
| Phase 8 | Auth state file paths (`~/.claude/auth.json` etc.) per platform for `doctor` subcommand |
| Phase 9 | Windows GitHub Actions runner config for subprocess tests; PyPI Trusted Publishing setup if pulled forward |

## Session Continuity

**Last action (07-02):** Executed Phase 7 plan 07-02 (Gemini + Codex adapter test suites + headline codex#19945 regression test). Two new files (`tests/test_adapter_gemini.py` 261 lines / 10132 bytes / LF-only / ASCII-only and `tests/test_adapter_codex.py` 333 lines / 13478 bytes / LF-only / ASCII-only) mirror `tests/test_adapter_claude.py` 1:1 with vendor swaps to `["gemini", "-p"]` and `["codex", "exec"]` argv shapes. Both files cover the same 6 paths via pytest-subprocess `fp` fixture: happy path with stdin pipe verification, list-form argv (defensively asserted via `fp.calls`), empty-stdout with rc=0 -> AdapterError (Pitfall #2 inherited mixin defense), whitespace-only stdout same defense, FileNotFoundError -> AdapterAuthError (via `monkeypatch.setattr(subprocess, "Popen", ...)`), parametrized auth-marker substring case-insensitive matching with vendor-specific markers including backticks, TimeoutExpired -> `_kill_process_tree` recorded + AdapterError. Gemini auth-marker parametrize has 5 entries; Codex has 4 entries. The CodexAdapter test file ALSO contains the **headline** `test_codex_empty_stdout_bug_regression` at line 129 explicitly documenting Pitfall #2 / openai/codex#19945 with the bug source URL, the bug shape, the defense location, and a "what the test pins vs deliberately does NOT pin" section. The headline regression test passes WITHOUT any Codex-specific defensive code in `CodexAdapter` -- the inherited mixin's empty-stdout defense from Phase 4 catches it directly. Total project test count: **50 -> 72** (+22 new = 11 gemini + 11 codex; zero regression). All 13 plan-level verification gates PASS: pytest 72/72; ruff clean; TST-05 lint still 3/3 PASS; LF-only + ASCII-only on disk; staged blobs LF-only despite `core.autocrlf=true`; `def test_codex_empty_stdout_bug_regression` present; `openai/codex#19945` literal present 3 times in codex test file; zero deletions; both files independently runnable in isolation. **Zero Rule-N deviations** -- plan executed verbatim. Atomic commits: `4377a27` (test(07-02): add GeminiAdapter test suite covering ADP-06) + `e538e88` (test(07-02): add CodexAdapter test suite covering ADP-07 and openai/codex#19945 regression). SUMMARY at `.planning/phases/07-gemini-codex-adapters/07-02-SUMMARY.md`. **ADP-06 and ADP-07 are now both IMPLEMENTATION-verified (07-01) AND TEST-verified (07-02), closing Phase 7 fully.** Phase 8 (CLI Surface) is now unblocked.

**Next action:** Plan / execute Phase 8 (CLI Surface & `debate` Preset). Phase 8 depends on Phase 6 (orchestrator `run()`) and Phase 7 (all three adapters) — both prerequisites now satisfied. CLI-01..CLI-11 + PRE-01 are 12 requirements covering `ultra-claude run`/`doctor`/`--version`/`--help` plus the bundled `presets/debate.yaml`. Run `/gsd-plan-phase 8` to decompose Phase 8 into executable plans. Phase 1 autonomous portion still complete; PKG-05 still pending user `twine upload`.

**Previous (07-01) action:** Executed Phase 7 plan 07-01 (Gemini + Codex adapter source files + registry wiring). Two new files (`src/ultra_claude/adapters/gemini.py` 94 lines / 4123 bytes / LF-only / ASCII-only and `src/ultra_claude/adapters/codex.py` 100 lines / 4339 bytes / LF-only / ASCII-only) define `class GeminiAdapter(_SubprocessAdapterMixin)` and `class CodexAdapter(_SubprocessAdapterMixin)` mirroring the proven `ClaudeAdapter` pattern with vendor-specific deltas only (class name, name/cli_name literal, auth_error_markers tuple, argv list literal). Both adapters delegate to `_run_subprocess(argv, prompt, timeout)` in a one-line `invoke()`; argv is a hard-coded literal list (`["gemini", "-p"]` / `["codex", "exec"]`) and the prompt is piped via stdin (Pitfall #1 mitigation, inherited from mixin). Codex module docstring contains the literal `openai/codex#19945` per locked decision D-03 — documenting why the inherited Pitfall #2 empty-stdout defense matters specifically for this adapter; NO Codex-specific defensive code added because the Phase 4 mixin already catches the regression for every adapter. `auth_error_markers` tuples KEPT DISTINCT per CLI vendor per locked decision D-02 (Gemini: 4 markers including `"please run \`gemini auth login\`"` and `"no credentials"`; Codex: 3 markers including `"please run \`codex login\`"`). Both files contain zero direct `import subprocess` / `from subprocess` references — TST-05 lint confirmed 3/3 PASS. `src/ultra_claude/adapters/__init__.py` extended (1798 bytes, 40 lines) from a 3-entry to a 5-entry `__all__` in roadmap-introduction order with `# noqa: RUF022` on the OPENING bracket line; alphabetical import-block ordering produces `.base / .claude / .codex / .gemini` above `__all__`. `src/ultra_claude/registry.py` rewritten (1998 bytes, 51 lines) — `NotImplementedError` branch removed entirely; replaced with `if adapter_kind == "gemini": return GeminiAdapter()` / `if adapter_kind == "codex": return CodexAdapter()`; module docstring tense changed from "Phase 7 will extend..." to "After Phase 7 ... all three adapter literals return real instances". All plan-level verification gates PASS: headline structural / registry check prints `OK: structural / registry checks pass`; `pytest tests/` 50/50 PASS (zero regression); `mypy --strict src/ultra_claude` clean (12 source files = 10 prior + gemini.py + codex.py); `ruff check` clean on the 4 modified/new files; `pytest tests/test_subprocess_lint.py` 3/3 PASS; LF-only + ASCII-only on disk on all 4 files; staged blobs LF-only despite `core.autocrlf=true`; `openai/codex#19945` literal present in codex.py module docstring; exactly 4 working-tree changes after the plan (2 new + 2 modified). One Rule-N deviation: **Rule 3 - Blocking**: ruff 0.15.8 RUF022 + RUF100 dual-fire on plan-suggested closing-bracket placement of `# noqa: RUF022` for multi-line `__all__`; moved noqa to the OPENING bracket line and ruff cleared. 4 pre-existing ruff errors in Phase 2 files (`config.py` RUF022 + UP037; `test_config.py` I001 + F401) confirmed pre-existing via `git stash` round-trip and logged at `.planning/phases/07-gemini-codex-adapters/deferred-items.md` as out-of-scope. ADP-06 and ADP-07 marked complete in REQUIREMENTS.md. Atomic commits: `4a09f27` (feat(07-01): add GeminiAdapter and CodexAdapter, 2 files / 194 insertions) + `5f067c1` (feat(07-01): wire Gemini/Codex into adapters package and registry, 2 files / 28 insertions / 19 deletions). SUMMARY at `.planning/phases/07-gemini-codex-adapters/07-01-SUMMARY.md`. Plan 07-01 CLOSED. Requirements: **ADP-06 + ADP-07 implementation-level COMPLETE** (boxes checked in REQUIREMENTS.md); test verification by plan 07-02 closes the loop.

**Previous (06-02) action:** Executed Phase 6 plan 06-02 (Orchestrator test suite with FakeAdapter). Created one new file `tests/test_orchestrator.py` (17306 bytes, 460 lines, LF-only, ASCII-only, UTF-8) defining `class FakeAdapter` plus 8 test functions covering every ORC-01..ORC-06 path. **Total project test count: 42 -> 50** (zero regression). Atomic commit: `747f003`. Phase 6 fully CLOSED.

**Files in scope:**

- `.planning/PROJECT.md` — core value, constraints, key decisions
- `.planning/REQUIREMENTS.md` — 58 v1 requirements mapped 100% to phases (32 complete; 0 partial; 1 deferred-to-user — PKG-05)
- `.planning/ROADMAP.md` — 9-phase structure with goal-backward success criteria
- `.planning/STATE.md` — this file
- `.planning/phases/01-project-skeleton-pypi-name-reservation/PUBLISH.md` — operator runbook for the deferred user action (PKG-05)
- `.planning/phases/02-config-schema-yaml-loader/02-01-PLAN.md` — completed plan
- `.planning/phases/02-config-schema-yaml-loader/02-01-SUMMARY.md` — completion summary for 02-01
- `.planning/phases/02-config-schema-yaml-loader/02-02-PLAN.md` — completed plan
- `.planning/phases/02-config-schema-yaml-loader/02-02-SUMMARY.md` — completion summary for 02-02
- `.planning/phases/02-config-schema-yaml-loader/02-CONTEXT.md` — phase 2 context
- `.planning/phases/02-config-schema-yaml-loader/deferred-items.md` — out-of-scope discoveries (autocrlf on Windows)
- `.planning/phases/03-transcript-module/03-CONTEXT.md` — phase 3 context (decisions, code insights, specifics)
- `.planning/phases/03-transcript-module/03-01-PLAN.md` — completed plan
- `.planning/phases/03-transcript-module/03-01-SUMMARY.md` — completion summary for 03-01
- `src/ultra_claude/exceptions.py` — landed in plan 02-01; extended in plan 04-01 with `AdapterError` + `AdapterAuthError`
- `src/ultra_claude/config.py` — landed in plan 02-02
- `src/ultra_claude/transcript.py` — landed in plan 03-01, consumed by Phase 5+ (Transcript + TurnRecord) and Phase 6 (orchestrator's append-as-you-go writer)
- `src/ultra_claude/adapters/__init__.py` — landed in plan 04-01, updated in 04-02 to re-export `ClaudeAdapter`; will gain `GeminiAdapter`/`CodexAdapter` in Phase 7
- `src/ultra_claude/adapters/base.py` — landed in plan 04-01; the choke point for every ultra-claude adapter (Adapter Protocol + safe-subprocess mixin); consumed by 04-02 (ClaudeAdapter — landed), 04-03 (tests + TST-05 lint), Phase 7 (Gemini/Codex adapters)
- `src/ultra_claude/adapters/claude.py` — landed in plan 04-02; first concrete adapter, wraps `claude -p` via the mixin; consumed by 04-03 tests and Phase 6 orchestrator (via registry)
- `.planning/phases/04-adapter-protocol-claudeadapter/04-CONTEXT.md` — phase 4 context (decisions, code insights, specifics)
- `.planning/phases/04-adapter-protocol-claudeadapter/04-01-PLAN.md` — completed plan
- `.planning/phases/04-adapter-protocol-claudeadapter/04-01-SUMMARY.md` — completion summary for 04-01
- `.planning/phases/04-adapter-protocol-claudeadapter/04-02-PLAN.md` — completed plan
- `.planning/phases/04-adapter-protocol-claudeadapter/04-02-SUMMARY.md` — completion summary for 04-02
- `.planning/phases/04-adapter-protocol-claudeadapter/04-03-PLAN.md` — completed plan
- `.planning/phases/04-adapter-protocol-claudeadapter/04-03-SUMMARY.md` — completion summary for 04-03
- `.planning/phases/04-adapter-protocol-claudeadapter/deferred-items.md` — out-of-scope discoveries (4 pre-existing ruff errors in Phase 2 files; logged 2026-05-02 by 04-03 executor; the 2 src/ultra_claude/config.py items are still surfaced by Phase 6 verification but remain deferred)
- `tests/__init__.py` — package marker (Phase 2)
- `tests/test_config.py` — landed in plan 02-02 (8 tests, all PASS)
- `tests/test_transcript.py` — landed in plan 03-01 (8 tests, all PASS)
- `tests/test_adapters_base.py` — landed in plan 04-03 (7 tests, all PASS)
- `tests/test_adapter_claude.py` — landed in plan 04-03 (10 tests, all PASS)
- `tests/test_subprocess_lint.py` — landed in plan 04-03 (3 tests, all PASS; TST-05 lint tripwire)
- `src/ultra_claude/stop_conditions.py` — landed in plan 05-01 (181 lines, 7142 bytes); StopCondition Protocol + Keyword + MaxTurns + AnyOf; consumed by Phase 6 orchestrator
- `tests/test_stop_conditions.py` — landed in plan 05-01 (6 tests, all PASS; STP-01..05 + Pitfall #4)
- `.planning/phases/05-stop-conditions/05-CONTEXT.md` — phase 5 context (decisions, code insights, specifics)
- `.planning/phases/05-stop-conditions/05-01-PLAN.md` — completed plan
- `.planning/phases/05-stop-conditions/05-01-SUMMARY.md` — completion summary for 05-01
- `src/ultra_claude/registry.py` — newly landed in plan 06-01 (56 lines, 2286 bytes); `get_adapter()` dispatcher; consumed by orchestrator.run() and (in Phase 7) extended for Gemini/Codex
- `src/ultra_claude/orchestrator.py` — newly landed in plan 06-01 (307 lines, 11876 bytes); `run(config, task) -> Path` entry point; consumed by Phase 8 CLI as a thin wrapper
- `.planning/phases/06-orchestrator-loop/06-CONTEXT.md` — phase 6 context (decisions, code insights, specifics)
- `.planning/phases/06-orchestrator-loop/06-01-PLAN.md` — completed plan
- `.planning/phases/06-orchestrator-loop/06-01-SUMMARY.md` — completion summary for 06-01
- `.planning/phases/06-orchestrator-loop/06-02-PLAN.md` — completed plan (this plan)
- `.planning/phases/06-orchestrator-loop/06-02-SUMMARY.md` — completion summary for 06-02 (this plan)
- `tests/test_orchestrator.py` — newly landed in plan 06-02 (460 lines, 17306 bytes; FakeAdapter helper + 8 tests covering ORC-01..ORC-06)
- `src/ultra_claude/adapters/gemini.py` — landed in plan 07-01 (94 lines, 4123 bytes); GeminiAdapter wrapping `gemini -p` via the mixin; mocked-subprocess test coverage landed in plan 07-02
- `src/ultra_claude/adapters/codex.py` — landed in plan 07-01 (100 lines, 4339 bytes); CodexAdapter wrapping `codex exec` via the mixin; module docstring references openai/codex#19945 (D-03); mocked-subprocess test coverage including the headline test_codex_empty_stdout_bug_regression landed in plan 07-02
- `tests/test_adapter_gemini.py` — newly landed in plan 07-02 (261 lines, 10132 bytes; 6 test functions, 11 collected; ADP-06 test verification)
- `tests/test_adapter_codex.py` — newly landed in plan 07-02 (333 lines, 13478 bytes; 7 test functions, 11 collected; ADP-07 test verification + headline test_codex_empty_stdout_bug_regression documenting Pitfall #2 / openai/codex#19945)
- `.planning/phases/07-gemini-codex-adapters/07-CONTEXT.md` — phase 7 context (decisions, code insights, specifics)
- `.planning/phases/07-gemini-codex-adapters/07-01-PLAN.md` — completed plan
- `.planning/phases/07-gemini-codex-adapters/07-01-SUMMARY.md` — completion summary for 07-01
- `.planning/phases/07-gemini-codex-adapters/07-02-PLAN.md` — completed plan (this plan)
- `.planning/phases/07-gemini-codex-adapters/07-02-SUMMARY.md` — completion summary for 07-02 (this plan)
- `.planning/phases/07-gemini-codex-adapters/deferred-items.md` — out-of-scope discoveries (4 pre-existing ruff errors in Phase 2 files; logged 2026-05-02 by 07-01 executor)
- `.planning/research/{SUMMARY,STACK,ARCHITECTURE,PITFALLS,FEATURES}.md` — context for plan-time research-flagged phases

---
*State initialized: 2026-05-02 after roadmap creation*
*Plan 01-01 completed: 2026-05-02 — commits 562d05e (chore: scaffolding files) + 2b15b36 (feat: __version__ stub)*
*Plan 01-02 completed: 2026-05-02 — commit b9bf3c5 (feat: pyproject.toml with hatchling backend, pinned deps, tool config)*
*Plan 01-03 completed: 2026-05-02 — commits 3e31832 (chore: .gitignore .smoke-venv defensive add) + e96ccb6 (docs: PUBLISH.md runbook); dist/ artifacts produced and smoke-tested locally; PKG-05 deferred to user action*
*Plan 02-01 completed: 2026-05-02 — commit ddfca71 (feat: add ConfigError exception class); CFG-03 partial (foundation; full delivery in 02-02)*
*Plan 02-02 completed: 2026-05-02 — commits e97325a (feat: add config schema and YAML loader) + 5c272f0 (test: add config validation test suite); CFG-01..CFG-05 all COMPLETE; 8/8 tests PASS; Phase 2 fully CLOSED*
*Plan 03-01 completed: 2026-05-02 — commits 88b6186 (feat: add transcript module with TurnRecord + Transcript classes) + 6230667 (test: add transcript test suite covering TRX-01..TRX-05); TRX-01..TRX-05 all COMPLETE; 8/8 transcript tests PASS + 16/16 full suite PASS; Phase 3 fully CLOSED*
*Plan 04-01 completed: 2026-05-02 — commits e4423d0 (feat: add AdapterError and AdapterAuthError exceptions) + eceb9da (feat: add Adapter Protocol and _SubprocessAdapterMixin); ADP-01..04, ADP-08 all COMPLETE; mypy --strict + ruff clean across all 3 files; 16/16 existing tests still PASS (zero regression); 6 inline integration smoke checks PASS; Phase 4 1/3 plans complete*
*Plan 04-02 completed: 2026-05-02 — commits 85e1c8f (feat(04-02): add ClaudeAdapter wrapping `claude -p` via _SubprocessAdapterMixin) + 40dd2ab (feat(04-02): re-export ClaudeAdapter from adapters package); ADP-05 COMPLETE; mypy --strict + ruff clean across all 4 files (exceptions.py, adapters/__init__.py, adapters/base.py, adapters/claude.py); 16/16 existing tests still PASS (zero regression); zero direct subprocess imports in claude.py; Phase 4 2/3 plans complete*
*Plan 04-03 completed: 2026-05-02 — commits ab17d77 (test(04-03): add Adapter Protocol structural typing tests) + e0ea60e (test(04-03): add ClaudeAdapter behaviour tests using pytest-subprocess fp fixture) + e16c4f9 (test(04-03): add TST-05 ast-walking subprocess-contract lint test); TST-05 COMPLETE; ADP-01..05 + ADP-08 verified by 17 executable tests; 36/36 full suite PASS (16 prior + 20 new; zero regression); mypy --strict on src/ultra_claude clean (7 files); ruff clean on the 3 new test files AND src/ultra_claude/adapters; LF-only + ASCII-only on disk; manual paranoia check confirmed TST-05 fires on synthetic bad scratch (both missing-kwargs and shell=True variants); Phase 4 fully CLOSED (3/3 plans, 7/7 ADP+TST requirements complete)*
*Plan 05-01 completed: 2026-05-02 — commits e56a779 (feat(05-01): add StopCondition Protocol + Keyword + MaxTurns + AnyOf) + 9dbc164 (test(05-01): add stop_conditions test suite covering STP-01..STP-05 + Pitfall #4); STP-01..STP-05 all COMPLETE; 6 new tests covering Protocol structural typing + anchored regex Pitfall #4 mitigation + unanimity-window n=2/m=2 positive/negative + MaxTurns boundary + AnyOf short-circuit; 42/42 full suite PASS (36 prior + 6 new; zero regression); mypy --strict on src/ultra_claude clean (8 files; was 7); ruff clean on both new files; LF-only + ASCII-only on disk; runtime_checkable Protocol smoke test PASSES; TST-05 lint test still passes (free regression check); zero Rule-N deviations during execution (anticipated `# noqa: RUF022` applied as documented); Phase 5 fully CLOSED (1/1 plan, 5/5 STP requirements complete)*
*Plan 06-01 completed: 2026-05-02 — commits 8cfee40 (feat(06-01): add adapter registry dispatcher) + b9b80b3 (feat(06-01): add orchestrator run() function); ORC-01..ORC-06 satisfied at IMPLEMENTATION level; 363 lines added (registry.py 56 + orchestrator.py 307); 5/5 end-to-end smoke checks PASS via inline `python -c`; 42/42 full suite PASS (zero regression); mypy --strict on src/ultra_claude clean (10 modules; was 8); ruff clean on the 2 new files; LF-only + ASCII-only on disk; staged blobs LF-only despite `core.autocrlf=true` host; zero Rule-N deviations during execution; Phase 6 progress: 1/2 plans*
*Plan 06-02 completed: 2026-05-02 — commit 747f003 (test(06-02): add orchestrator test suite covering ORC-01..ORC-06); ORC-01..ORC-06 all COMPLETE (boxes checked in REQUIREMENTS.md); 8 new tests via FakeAdapter helper covering round-robin / GOAL ANCHOR prompt assembly / transcript-so-far / Keyword unanimity early-stop / continue-on-error / abort-on-error / return-Path / stdout+caplog discipline; 460 lines added; 50/50 full suite PASS (42 prior + 8 new; zero regression); mypy --strict on src/ultra_claude still clean (10 source files unchanged); ruff clean on the new file; LF-only + ASCII-only on disk; staged blob LF-only despite `core.autocrlf=true` host; 2 Rule-N deviations (Rule 1: replaced capsys.err with caplog in test 8 because pytest's logging plugin diverts records — supersedes plan's `propagate=False` recommendation; Rule 3: ruff I001 1-blank-line fix in import block — both fixes folded into commit 747f003 before commit); Phase 6 fully CLOSED (2/2 plans, 6/6 ORC requirements complete); Phase 7 (Gemini + Codex Adapters) UNBLOCKED*
*Plan 06-01 completed: 2026-05-02 — commits 8cfee40 (feat(06-01): add adapter registry dispatcher) + b9b80b3 (feat(06-01): add orchestrator run() function); ORC-01..ORC-06 satisfied at IMPLEMENTATION level (executable test verification lands in 06-02); 363 lines added across 2 new files (registry.py 56 lines / 2286 bytes + orchestrator.py 307 lines / 11876 bytes; both LF-only + ASCII-only on disk and in staged blob despite `core.autocrlf=true` host); 0 new tests (06-02 lands tests); 5/5 end-to-end smoke checks PASS via inline `python -c` (round-robin, GOAL ANCHOR, continue-on-error, abort-on-error, default-path + Keyword unanimity stop); 42/42 full suite PASS (zero regression — orchestrator/registry only ADD code); mypy --strict on src/ultra_claude clean (10 modules; was 8); ruff clean on the 2 new files; TST-05 lint test still passes; zero Rule-N deviations during execution (anticipated TRY401 pre-emption + `core.autocrlf=true` host compensation via `Path.write_bytes` were called out in the plan's Task 1/Task 2 notes); 2 pre-existing config.py ruff errors (RUF022 + UP037 from Phase 2 commit e97325a) remain deferred per scope-boundary rule; Phase 6 1/2 plans complete*
*Plan 07-01 completed: 2026-05-02 — commits 4a09f27 (feat(07-01): add GeminiAdapter and CodexAdapter) + 5f067c1 (feat(07-01): wire Gemini/Codex into adapters package and registry); ADP-06, ADP-07 implementation-level COMPLETE; 222 lines added across 4 files (2 new: gemini.py 94 lines + codex.py 100 lines; 2 modified: adapters/__init__.py 5-entry __all__ + registry.py concrete instantiation); 50/50 full suite PASS (zero regression); mypy --strict clean (12 source files); ruff clean; LF-only + ASCII-only; codex.py module docstring references openai/codex#19945 (D-03); 1 Rule-N deviation (Rule 3: # noqa: RUF022 placement on multi-line __all__); Phase 7 progress: 1/2 plans*
*Plan 07-02 completed: 2026-05-02 — commits 4377a27 (test(07-02): add GeminiAdapter test suite covering ADP-06) + e538e88 (test(07-02): add CodexAdapter test suite covering ADP-07 and openai/codex#19945 regression); ADP-06 + ADP-07 now both IMPLEMENTATION + TEST verified; 22 new tests across 2 new files (test_adapter_gemini.py 261 lines / 11 collected + test_adapter_codex.py 333 lines / 11 collected including the headline test_codex_empty_stdout_bug_regression at line 129); 72/72 full suite PASS (50 prior + 22 new; zero regression); ruff clean on both new test files; TST-05 lint still 3/3 PASS; LF-only + ASCII-only on disk; staged blobs LF-only despite `core.autocrlf=true`; `def test_codex_empty_stdout_bug_regression` present; `openai/codex#19945` literal present 3 times in codex test file; zero Rule-N deviations (plan executed verbatim); zero source changes (test-only plan); zero deletions; Phase 7 fully CLOSED (2/2 plans, 2/2 ADP requirements complete); Phase 8 (CLI Surface) UNBLOCKED*
